# 🤖 Стратегия использования агентов для исправлений

## 📋 Контекст

У нас есть 3 проблемы для исправления по плану **Вариант A** (от простого к сложному):
1. Счетчик questions_asked (10-15 мин)
2. Qdrant URL (15-20 мин)
3. AnswerAnalyzer методы (20-30 мин)

**Вопрос:** Какие агенты использовать и зачем?

---

## 🎯 Доступные агенты из SDK

Из `.claude/agents/` у нас есть:
- **selfology-architect.md** - универсальный агент с пониманием архитектуры

Из Agent SDK есть специализированные агенты:
- **backend-architect** - проектирование API, БД, серверная логика
- **frontend-developer** - UI компоненты, React, Vue
- **test-writer-fixer** - тестирование, фиксы тестов
- **devops-automator** - CI/CD, инфраструктура
- **rapid-prototyper** - быстрое прототипирование
- и др.

---

## 🔍 Анализ: Нужны ли специализированные агенты?

### Проблема #1: Счетчик questions_asked

**Затронутые компоненты:**
- `database/onboarding_dao.py` - добавить метод SQL
- `services/onboarding/orchestrator.py` - вызвать метод

**Сложность:** Низкая (простой SQL UPDATE + вызов метода)

**Нужен ли специализированный агент?**
❌ **НЕТ** - это слишком просто для отдельного агента.

**Причины:**
1. Один файл БД (DAO) - простое добавление метода
2. Один файл бизнес-логики - простой вызов
3. Нет архитектурных решений - чистый императивный код
4. Нет тестов для исправления (пока)

**Рекомендация:** Использовать **selfology-architect** напрямую.

---

### Проблема #2: Qdrant URL

**Затронутые компоненты:**
- `analysis/embedding_creator.py` - исправить инициализацию Qdrant client

**Сложность:** Средняя (нужно понять Qdrant API + error handling)

**Нужен ли специализированный агент?**
🤔 **МОЖЕТ БЫТЬ** - если бы у нас был **devops-automator** или **infrastructure-maintainer**.

**НО:**
- Это не deployment проблема (devops)
- Это не infrastructure масштаба (monitoring)
- Это просто URL config + error handling в коде

**Рекомендация:** Использовать **selfology-architect** напрямую.

**Почему не devops-automator:**
DevOps агент полезен для:
- Настройки docker-compose
- CI/CD pipelines
- Мониторинг инфраструктуры

Наша проблема = код Python, а не инфраструктура.

---

### Проблема #3: AnswerAnalyzer методы

**Затронутые компоненты:**
- `analysis/answer_analyzer.py` - добавить 2 метода с логикой

**Сложность:** Высокая (бизнес-логика анализа души)

**Нужен ли специализированный агент?**
🤔 **МОЖЕТ БЫТЬ** - если бы у нас был **ai-engineer**.

**ai-engineer полезен для:**
- Интеграция LLM
- Prompt engineering
- ML pipelines
- AI feature implementation

**Наша задача:**
- Добавить метод `_estimate_user_energy()` - логика на Python
- Добавить метод `_get_emergency_analysis()` - fallback структура данных

**Это AI?** Да, но не интеграция нового AI, а **внутренняя логика** вокруг существующего AI.

**Рекомендация:** Использовать **selfology-architect** напрямую.

**Почему не ai-engineer:**
- Не добавляем новые AI модели
- Не меняем промпты
- Просто добавляем вспомогательные методы в существующий класс

---

## 💡 Вывод: Один агент для всех задач

### ✅ Решение: Использовать **selfology-architect** для всех 3 проблем

**Почему?**

#### 1. **Контекстная связность**
Все 3 проблемы - части **одной системы онбординга**:
- Счетчик → session management
- Qdrant → vector storage для analysis
- AnswerAnalyzer → core analysis logic

Один агент, который знает **всю систему целиком**, сделает лучше чем 3 разных.

#### 2. **Архитектурное понимание**
`selfology-architect` из `.claude/agents/selfology-architect.md` уже знает:
- Event-driven архитектуру
- Схему БД (selfology schema)
- Систему анализа (Analysis Worker, Soul Architect)
- Онбординг flow

#### 3. **Простота задач**
Ни одна из задач не требует **глубокой специализации**:
- SQL UPDATE - базовый SQL
- URL config - базовый Python
- Методы анализа - логика на Python (не ML integration)

#### 4. **Скорость**
Запуск одного агента 3 раза **быстрее** чем:
- Запуск 3 разных агентов
- Переключение контекста между ними
- Синхронизация результатов

#### 5. **Последовательность важна**
Фиксы идут **по порядку** (Вариант A):
1. Счетчик → проверили → работает ✅
2. Qdrant → проверили → работает ✅
3. AnswerAnalyzer → проверили → работает ✅

Один агент держит **контекст между фиксами**.

---

## 🎯 Стратегия использования selfology-architect

### Фаза 1: Счетчик (10-15 мин)

**Агент:** selfology-architect
**Контекст из CLAUDE.md:** ✅ Знает про БД, онбординг, архитектуру

**Команда:**
```bash
claude --agent selfology-architect "
Проблема: questions_asked не инкрементируется при отправке вопроса.

Задача:
1. Проверь OnboardingDAO - есть ли метод increment_questions_asked
2. Если нет - добавь метод с SQL UPDATE
3. Найди в orchestrator.py где отправляется вопрос (start_onboarding и process_answer)
4. Добавь вызов increment_questions_asked после выбора вопроса
5. Покажи изменения

Контекст: DETAILED_FIX_PLAN_2025-10-01.md (Проблема #2)
"
```

**Что агент сделает:**
1. ✅ Прочитает `database/onboarding_dao.py`
2. ✅ Найдет что метода нет
3. ✅ Добавит метод `increment_questions_asked()`
4. ✅ Прочитает `orchestrator.py`
5. ✅ Найдет `start_onboarding()` и места после выбора вопроса
6. ✅ Добавит вызовы метода
7. ✅ Покажет diff

**Проверка:**
```sql
SELECT questions_asked FROM selfology.onboarding_sessions WHERE id = 4;
-- Должно быть > 0
```

---

### Фаза 2: Qdrant (15-20 мин)

**Агент:** selfology-architect (тот же)
**Контекст:** Уже знает что мы чиним онбординг, видел DAO и orchestrator

**Команда:**
```bash
claude --agent selfology-architect "
Проблема: Qdrant коллекции не создаются из-за неправильного URL.

Задача:
1. Найди в embedding_creator.py где инициализируется qdrant_client
2. Добавь проверку: если URL содержит 'qdrant:6333', замени на 'localhost:6333'
3. Добавь try/except с логированием ошибок при подключении
4. Найди метод setup_qdrant_collections()
5. Убедись что ошибки НЕ подавляются (не используй pass в except)
6. Добавь проверку что коллекции реально созданы
7. Покажи изменения

Контекст: DETAILED_FIX_PLAN_2025-10-01.md (Проблема #3)
"
```

**Что агент сделает:**
1. ✅ Прочитает `embedding_creator.py`
2. ✅ Найдет `__init__` с `qdrant_client = None`
3. ✅ Добавит правильную инициализацию с URL fix
4. ✅ Найдет `setup_qdrant_collections()`
5. ✅ Проверит error handling
6. ✅ Исправит если ошибки подавляются
7. ✅ Покажет diff

**Проверка:**
```bash
curl localhost:6333/collections | jq '.result.collections[].name'
# Должны быть: selfology_answers_small, medium, large
```

---

### Фаза 3: AnswerAnalyzer (20-30 мин)

**Агент:** selfology-architect (тот же)
**Контекст:** Знает систему, видел БД, видел Qdrant, понимает flow

**Команда:**
```bash
claude --agent selfology-architect "
Проблема: AnswerAnalyzer падает из-за отсутствующих методов _estimate_user_energy и _get_emergency_analysis.

Задача:
1. Прочитай answer_analyzer.py полностью
2. Найди где вызываются эти методы (строка 177 и 143)
3. Добавь метод _estimate_user_energy(context) -> float:
   - Анализ длины ответа (> 200 = +0.2, < 30 = -0.2)
   - Анализ скорости ответа (< 30s = +0.1, > 120s = -0.1)
   - Учет прогресса (вопрос N / 100 = fatigue factor)
   - Учет fatigue_level из контекста
   - Вернуть float [0.0, 1.0]

4. Добавь метод _get_emergency_analysis(question_data, user_answer, user_context, error_message):
   - Нейтральный instant_feedback
   - Traits все = 0.5
   - Простой psychological_insights
   - Флаг is_emergency_analysis = True
   - Логировать warning с ошибкой

5. Убедись что методы соответствуют сигнатурам в коде
6. Покажи изменения

Контекст: DETAILED_FIX_PLAN_2025-10-01.md (Проблема #1)
"
```

**Что агент сделает:**
1. ✅ Прочитает весь `answer_analyzer.py` (693 строки)
2. ✅ Найдет строки 177 и 143 с вызовами
3. ✅ Создаст метод `_estimate_user_energy()` с логикой
4. ✅ Создаст метод `_get_emergency_analysis()` с fallback
5. ✅ Проверит сигнатуры методов
6. ✅ Покажет diff
7. ✅ Hot reload автоматически применит изменения

**Проверка:**
```bash
# Отправить новый ответ в боте
# Проверить БД:
SELECT COUNT(*) FROM selfology.answer_analysis;
-- Должно быть > 0
```

---

## 🔄 Почему НЕ использовать несколько агентов?

### ❌ Вариант "Много агентов"

**Гипотетический план:**
1. **backend-architect** - для счетчика SQL
2. **devops-automator** - для Qdrant URL
3. **ai-engineer** - для AnswerAnalyzer

**Проблемы:**

#### 1. **Потеря контекста между агентами**
```
backend-architect чинит БД
  ↓
devops-automator не знает что мы чинили БД
  ↓
ai-engineer не знает что мы чинили БД и Qdrant
  ↓
Риск конфликтов и дублирования
```

#### 2. **Overhead на запуск**
- Каждый агент = новая инициализация
- Каждый агент = загрузка контекста заново
- Каждый агент = свой workflow

**Время:** 3 × (инициализация + контекст) = медленнее

#### 3. **Специализация избыточна**
Наши задачи **слишком простые** для узкой специализации:
- SQL UPDATE - не нужен backend-architect
- URL config - не нужен devops-automator
- Python методы - не нужен ai-engineer

#### 4. **Риск несоответствия архитектуре**
Специализированные агенты могут не знать:
- Event-driven архитектуру Selfology
- Схему `selfology` (не `public`)
- Smart Mix алгоритм
- Двухфазный анализ (instant + deep)

**selfology-architect знает всё это из CLAUDE.md!**

---

## ✅ Преимущества "Один агент"

### 1. **Единый контекст**
Агент помнит:
- Что мы чинили счетчик → понимает зачем Qdrant → понимает зачем AnswerAnalyzer
- Архитектуру системы → не предложит плохие решения
- Предыдущие фиксы → не сломает что уже починил

### 2. **Скорость**
```
1 агент × 3 задачи = быстро
vs
3 агента × 1 задача = медленно (инициализация × 3)
```

### 3. **Последовательность**
Фиксы проверяются **по порядку**:
```
Счетчик → ✅ работает → Qdrant → ✅ работает → AnswerAnalyzer → ✅ работает
```

Не:
```
Счетчик ✅
Qdrant ✅  } параллельно, потом обнаружили что не работает
AnswerAnalyzer ✅
```

### 4. **Понимание системы целиком**
selfology-architect из `.claude/agents/`:
- ✅ Знает event-driven архитектуру
- ✅ Знает системы (question_selection, analysis_worker, soul_architect)
- ✅ Знает правила (схема selfology, тесты после изменений)
- ✅ Знает Decision Tree для запросов

### 5. **Безопасность**
Агент **не сломает** другие части:
- Знает что системы общаются через события
- Не будет делать прямые вызовы между системами
- Проверит что не трогает чужие компоненты

---

## 🎯 Финальная стратегия

### ✅ Используем: selfology-architect (один агент)

**Последовательность:**
1. **Фикс #1:** Счетчик questions_asked
   - Проверка: SQL query
   - Результат: счетчик работает ✅

2. **Фикс #2:** Qdrant URL
   - Проверка: curl collections
   - Результат: коллекции созданы ✅

3. **Фикс #3:** AnswerAnalyzer методы
   - Проверка: новый ответ → анализ в БД
   - Результат: AI анализ работает ✅

**Общее время:** 45-65 минут (с проверками)

**Преимущества:**
- ✅ Единый контекст
- ✅ Понимание архитектуры
- ✅ Последовательное тестирование
- ✅ Безопасные изменения
- ✅ Быстрее чем 3 агента

---

## 📝 Команды для выполнения

### Фаза 1: Счетчик
```bash
claude --agent selfology-architect "
Исправь счетчик questions_asked согласно DETAILED_FIX_PLAN_2025-10-01.md (Проблема #2).
Добавь метод в OnboardingDAO и вызови в orchestrator.py после отправки вопроса.
"
```

### Фаза 2: Qdrant
```bash
claude --agent selfology-architect "
Исправь Qdrant URL для локального запуска согласно DETAILED_FIX_PLAN_2025-10-01.md (Проблема #3).
Замени qdrant:6333 на localhost:6333 и добавь error handling.
"
```

### Фаза 3: AnswerAnalyzer
```bash
claude --agent selfology-architect "
Добавь методы _estimate_user_energy и _get_emergency_analysis в AnswerAnalyzer согласно DETAILED_FIX_PLAN_2025-10-01.md (Проблема #1).
"
```

---

**Итого: 3 команды, 1 агент, чистая последовательность, полное понимание системы! 🚀**
