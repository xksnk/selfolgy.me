<img src="https://r2cdn.perplexity.ai/pplx-full-logo-primary-dark%402x.png" style="height:64px;margin-right:32px"/>

# Архитектура AI-коуча: структурированность vs гибкость

Вот структурированный анализ на основе коучинг-протоколов, терапевтических моделей и UX conversational AI.

## 1. Должны ли вопросы внутри блока идти строго последовательно?

**Ответ: Зависит от типа блока.**

### Типология блоков по зависимостям:

| Тип блока | Примеры | Порядок | Почему |
| :-- | :-- | :-- | :-- |
| **Линейный (зависимый)** | "История проблемы" → "Первый опыт" → "Паттерны" | ✅ Строго | Каждый уровень нужен для понимания следующего |
| **Фактологический (независимый)** | "Когда это началось?", "Кто был вовлечён?", "Где происходило?" | ⚠️ Мягко | Можно переставлять, но лучше придерживаться логики |
| **Рефлексивный (резонансный)** | "Что ты чувствовал?", "Что это значит?", "Как это влияет?" | 🔴 Обязательно | Порядок создаёт психологическую траекторию (от факта → к смыслу) |
| **Исследовательский (ветвящийся)** | "Какие есть варианты?", "Что ты забыл рассмотреть?", "Где противоречие?" | ⚠️ Гибко | Порядок менее критичен, но последовательность повышает полноту |

**Вывод для дизайна:**

- В каждом блоке указывай **уровень жёсткости** последовательности (hard/soft/flexible)
- Линейные блоки = якоря, их нельзя перепрыгивать
- Фактологические = основа для рефлексивных
- Рефлексивные = психологическая траектория (факт → чувства → смысл → действие)

***

## 2. Можно ли переключаться между блоками одной программы?

**Ответ: Да, но с архитектурными правилами.**

### Правила переключения:

```
БЛОК A (основание)
    ↓
БЛОК B, C, D (верхние уровни — можно перемешивать)
    ↓
БЛОК E (интеграция — нужна после B, C, D)

Нельзя: A → E (пропуск основания)
Можно: A → B → D → C → E (перестановка промежуточных)
```


### Когда гибкость работает лучше всего:

**Сценарий 1: Эмоциональное насыщение**

```
Клиент: "Я отвечу на вопрос про чувства позже, давайте о поведении"
→ Переключение между эмоциональным (Блок B) и поведенческим (Блок C)
→ Даёт ощущение контроля, снижает сопротивление
```

**Сценарий 2: Травматичный материал**

```
Вопрос активировал боль → AI должен предложить:
"Хочешь перейти в безопаснее место диалога? Вернёмся к этому позже"
→ Гибкость спасает терапевтический альянс
```

**Сценарий 3: Естественный ход мысли**

```
Клиент: "Я понял, что это связано с моим детством..."
→ AI видит скачок в глубину и адаптирует путь
→ Следует потоку, а не плану
```


### Архитектурные слои программы:

```yaml
Программа "Отношения и привязанность"
  Блок 1 (Foundation): "Мой опыт привязанности"
    - Автономны, последовательны
    - MUST-пройти перед всеми
  
  Блоки 2-5 (Exploration): "Паттерны", "Травмы", "Ресурсы", "Желания"
    - Могут перемешиваться
    - Рекомендуемый порядок, но не обязательный
    - Каждый опирается на Блок 1
  
  Блок 6 (Integration): "Новое видение привязанности"
    - Выполняется в конце
    - Интегрирует все остальное
```

**Вывод:**

- Обозначь каждый блок как **Foundation / Exploration / Integration**
- Foundation = обязательное основание, нельзя пропускать
- Exploration = гибко перемешивается (но в рекомендуемом порядке)
- Integration = якорь в конце

***

## 3. Можно ли миксовать вопросы из разных программ в одной сессии?

**Ответ: Нет (или очень ограниченно). Вот почему.**

### Проблемы микса программ:

| Проблема | Проявление | Риск |
| :-- | :-- | :-- |
| **Потеря контейнера** | "Мы про отношения, потом про кариеру, потом про семью" | 🔴 Высокий — фрагментация сознания |
| **Нарушение рефлексивной цепи** | Каждая программа строит свою нейросетевую карту → микс = белый шум | 🔴 Высокий — мозг переключается, теряет глубину |
| **Конфликт смыслов** | Программа А утверждает "Нужна самостоятельность", Программа Б — "Нужна близость" | 🟡 Средний — противоречия не обрабатываются |
| **Потеря терапевтического альянса** | Клиент теряет ощущение направления → "Что мы выясняем?" | 🔴 Высокий — снижает доверие |
| **Неполная интеграция** | Ни одна программа не завершена → висячие гештальты | 🔴 Высокий — когнитивный диссонанс |

### Когда микс может быть оправдан:

**1. Мостовые вопросы (3-5% сессии)**

```
Клиент на Программе "Самооценка" говорит о конфликте с партнёром
→ AI может спросить: "Это как-то связано с тем, как ты видишь себя 
   в отношениях?" (мостовой вопрос)
→ НО: продолжает программу, не переключается в "Отношения"
```

**2. Метаслой (рефлексия о рефлексии)**

```
Клиент: "Я заметил, что со своей самооценкой связана моя забота 
   о других"
→ AI: "Это серьёзное наблюдение. Запишу его. После этого блока 
   можем исследовать это в программе про границы"
→ Предлагает переход, но завершает текущую программу
```

**3. Кризисные ситуации (экстренный режим)**

```
Клиент: "У меня суицидальные мысли"
→ AI выходит из программы в протокол безопасности
→ После стабилизации — постепенное возвращение в программу
```


### Безопасная архитектура микса:

```
Сессия 1: Программа A (полностью или до Integration блока)
Сессия 2: Программа A (завершение если нужно)
Сессия 3: Программа B
    ↓
Сессия X: Метаслой "Интеграция программ" 
    (специальный блок, который соединяет A + B + C)
```

**Вывод:**

- Каждая программа = замкнутый контейнер
- Микс вызывает фрагментацию сознания и снижает эффект
- Исключение: мостовые вопросы внутри программы
- Синтез разных программ → отдельный метаслой после каждой

***

## 4. Как это влияет на глубину рефлексии и терапевтический эффект?

### Модель глубины (по Роджерсу, Бойдену, Крамеру):

```
УРОВЕНЬ 1: Сообщение фактов
  "Когда это было? Кто был?"
  Глубина: поверхностная
  
УРОВЕНЬ 2: Описание чувств
  "Что ты чувствовал? Как реагировал?"
  Глубина: поверхностно-средняя
  
УРОВЕНЬ 3: Выявление паттернов
  "Это повторяется? Когда началось?"
  Глубина: средняя
  
УРОВЕНЬ 4: Смысловое присвоение
  "Что это говорит о тебе? Что ты теперь понимаешь?"
  Глубина: глубокая
  
УРОВЕНЬ 5: Трансформационное озарение
  "Как это меняет твой выбор? Кто ты становишься?"
  Глубина: очень глубокая (зона рефлексии)
```


### Влияние структурированности на глубину:

**Сценарий A: Жёсткая последовательность (все блоки подряд)**

```
Программа "Страхи"
  Блок 1 → Блок 2 → Блок 3 → Интеграция
  
Эффект: ✅ Максимальная глубина
- Мозг выстраивает нейросетевую карту
- Каждый вопрос опирается на предыдущий
- Нет "прыжков" (снижают ясность)
- Интеграция происходит органично

Вердикт: Идеально для 📖 книги (читатель уже "вложился")
```

**Сценарий B: Мягкая гибкость (можно менять блоки 2-5)**

```
AI-диалог, клиент выбирает порядок

Эффект: ⚠️ Средняя глубина
+ Ощущение контроля (нужно для альянса)
+ Адаптация к сопротивлению
- Может быть "прыг-скок", теряется связность
- Интеграция труднее

Вердикт: Хорошо для 🤖 AI с навязчивыми рефлексиями
```

**Сценарий C: Жёсткая жёсткость (только 1 вариант)**

```
Клиент против: "Я не готов отвечать на этот вопрос"

Эффект: 🔴 Риск потери альянса
- Клиент чувствует себя "пациентом" AI
- Может закрыться или прервать сессию
- Глубина = ноль

Вердикт: ❌ Неприемлемо для 🤖 AI
```

**Сценарий D: Микс программ в одной сессии**

```
Программа "Отношения" + "Карьера" + "Самоценность"

Эффект: 🔴 Минимальная глубина
- Фрагментация: 3 разные нейросети одновременно
- Каждая программа строит свой смысловой контейнер
- Микс = "белый шум" для мозга
- Клиент уходит с ощущением: "Мы обсудили много, но не разобрались"
- Висячие гештальты активируют тревогу

Вердикт: ❌ Не рекомендуется
```


### Научная база (почему последовательность работает):

| Механизм | Исследование | Эффект |
| :-- | :-- | :-- |
| **Рабочая память** | Baddeley, working memory capacity | Человек удерживает 4±1 элемента. Линейный путь = минимум когнитивной нагрузки |
| **Глубокая обработка** | Craik \& Lockhart, levels of processing | Вопросы в последовательности вызывают глубокую обработку (vs поверхностную) |
| **Нейропластичность** | Siegel, interpersonal neurobiology | Повторение одного паттерна → укрепление синапсов. Микс = "шумовое возбуждение" |
| **Соотношение фигура-фон** | Гештальт-психология | Одна программа = фигура, остальное = фон. Микс = все элементы конкурируют |
| **Безопасность нервной системы** | Porges, polyvagal theory | Предсказуемость (линейный путь) = активация парасимпатики. Хаос = симпатика, сопротивление |


***

## Практические рекомендации для Selfology

### Для AI-коуча:

```yaml
Архитектура сессии:

1. Foundation блок (обязательная последовательность)
   - Правило: всегда сначала
   - Защита: "Начнём с основ — это поможет нам разобраться глубже"

2. Exploration блоки (мягкая гибкость)
   - Правило: рекомендуемый порядок, но клиент может просить другой
   - Защита: AI предлагает "Может, сначала разберёмся с чувствами? 
     Так будет безопаснее"
   - Честность: "Я предлагаю такой порядок, потому что..."

3. Адаптивные переходы (для сопротивления)
   - Если клиент закрывается: "Давай перейдём в безопаснее место"
   - Если активируется боль: "Хочешь паузу или переключение?"
   - Всегда: "Ты контролируешь темп"

4. Integration блок (обязательный, в конце)
   - Правило: после Exploration
   - Функция: собрать все в нарратив
   - Защита: "Теперь давай посмотрим, что мы выяснили вместе"

5. Метаслой (опционально, между программами)
   - После завершения программы A
   - Вопрос: "Что это открыло для тебя? Как это связано 
     с другими областями?"
   - ЗАТЕМ: предложить программу B или завершить сессию
```


### Для книги:

```yaml
Формат: Линейный путь (нет гибкости)

Структура:
- Каждая программа = замкнутая глава
- Блоки идут строго Foundation → Exploration → Integration
- Читатель может пропускать (его право), но это нарушит логику

Дизайн-сигналы:
- "Если пропустишь этот раздел, потеряешь суть" (для Foundation)
- "Можно прочитать в любом порядке" (если Exploration независимы)
- "Сначала закончи эту главу" (для Integration)

Защита в текстовой форме:
- Перекрёстные ссылки
- Иерархия заголовков (Foundation = H2, Exploration = H3)
- Ясная архитектура в предисловии
```


***

## Итоговая таблица (для дизайна Selfology):

| Параметр | 📖 Книга | 🤖 AI-коуч |
| :-- | :-- | :-- |
| **Порядок внутри блока** | Строгая последовательность | Рекомендуемый (с гибкостью) |
| **Переключение блоков** | Линейно (A→B→C) | Мягко (A→EXPLORE C/B/D→Integration) |
| **Микс программ** | ❌ Невозможен | ❌ Не рекомендуется (исключение: метаслой) |
| **Контроль над потоком** | У читателя (но расценивается как нарушение) | У клиента (ощущение). У AI (архитектура) |
| **Глубина рефлексии** | Максимальная (если читает правильно) | Средне-высокая (баланс альянса и глубины) |
| **Терапевтический эффект** | Полный (при полном прохождении) | Достаточный (при соблюдении архитектуры) |


***

## Вердикт для Selfology

✅ **Рекомендуемая модель для AI-коуча:**

1. **Строгая последовательность Foundation блоков** (нельзя пропускать)
2. **Мягкая гибкость Exploration блоков** (рекомендуемый порядок, но клиент может менять)
3. **Обязательная Integration** (в конце, собирает всё)
4. **Адаптивные переходы** (при сопротивлении или боли)
5. **Метаслой между программами** (опционально, после завершения одной перед началом другой)
6. **Недопустимый микс программ** (кроме экстренных случаев)

**Почему это работает:**

- Баланс структурированности (глубина) и гибкости (альянс)
- Максимизирует терапевтический эффект
- Защищает от фрагментации сознания
- Работает и для интровертов (медленный ритм), и для экстравертов (ощущение выбора)

***

Нужны ли ещё примеры адаптивных переходов или дизайн-спецификации для кодирования этой логики в AI?

---

# Метаданные для Selfology: система максимальной эффективности

Отвечаю структурированно: анализ текущей системы → упрощение vs расширение → архитектура иерархии → готовые решения.

***

## 1. Сохранять ли текущую систему 17 метаданных?

**Ответ: Сохранять, но переструктурировать (17 → 9 критичных + 8 динамических)**

### Аудит текущей системы:

| Метаданные | Критичность | Почему | Статус |
| :-- | :-- | :-- | :-- |
| `journey_stage` | 🔴 КРИТИЧЕН | Определяет архитектуру траектории | ✅ Оставить |
| `depth_level` | 🔴 КРИТИЧЕН | Управляет QuestionRouter, защищает от скачков | ✅ Оставить |
| `domain` | 🟡 ВАЖЕН | Помогает отслеживать многогранность, но может выводиться | ⚠️ Переделать |
| `energy_dynamic` | 🔴 КРИТИЧЕН | FatigueDetector, безопасность клиента | ✅ Оставить |
| `complexity` | 🔴 КРИТИЧЕН | AIModelRouter (малые модели vs GPT-4o) | ✅ Оставить |
| `emotional_weight` | 🔴 КРИТИЧЕН | Отслеживание травмы, интенсивность | ✅ Оставить |
| `insight_potential` | 🟡 ВАЖЕН | Nice-to-have, может выводиться из depth_level | ⚠️ Убрать |
| `safety_level` | 🔴 КРИТИЧЕН | Защита от нежелательных эффектов | ✅ Оставить |
| `trust_requirement` | 🔴 КРИТИЧЕН | Управляет альянсом, контекстом | ✅ Оставить |
| `recommended_model` | 🔴 КРИТИЧЕН | Экономия токенов, качество ответа | ✅ Оставить |
| `batch_compatible` | 🟢 ОПЦИОНАЛЕН | Редко используется в conversational AI | ❌ Убрать |
| `requires_context` | 🔴 КРИТИЧЕН | Управляет памятью диалога, контекстом | ✅ Оставить |
| `classification.*` | 🔴 КРИТИЧЕН | Основа для всех маршрутов | ✅ Оставить |
| `psychology.*` | 🔴 КРИТИЧЕН | Ядро системы | ✅ Оставить |

### Итог аудита:

**Убрать:** `insight_potential`, `batch_compatible` (2)
**Оставить:** 15 параметров
**Добавить:** 2 новых для блочной архитектуры

***

## 2. Автоматическая генерация метаданных для новых вопросов

**Рекомендация: Гибридный подход (AI + правила на основе позиции)**

### Архитектура генерации:

```
Новый вопрос поступает
    ↓
Шаг 1: ПРАВИЛА НА ОСНОВЕ ПОЗИЦИИ (детерминированные)
    ├─ Программа → journey_stage автоматически
    ├─ Блок (Foundation/Exploration/Integration) → depth_level диапазон
    ├─ Позиция в блоке → energy_dynamic примерный
    └─ Тип блока → safety_level минимум
    ↓
Шаг 2: AI-ГЕНЕРАЦИЯ (для сложных параметров)
    ├─ Анализ текста вопроса → emotional_weight, complexity
    ├─ Семантический анализ → domain, trust_requirement
    └─ Потенциал инсайтов → insight_potential из depth_level
    ↓
Шаг 3: ВЕРИФИКАЦИЯ ЧЕЛОВЕКОМ (критичные параметры)
    ├─ Клинический психолог проверяет emotional_weight, safety_level
    └─ При спорных случаях → переговор AI vs Психолог
    ↓
Результат: Вопрос с полными метаданными
```


***

### Вариант 1: AI-генерация (промпт → JSON)

**Плюсы:** Быстро, адаптивно, гибко
**Минусы:** Требует верификации, может быть непоследовательно

**Готовый промпт для Claude / GPT-4o:**

```yaml
СИСТЕМА: Ты — метаданный анализатор для терапевтического коучинга.

ВХОДНЫЕ ДАННЫЕ:
  - Вопрос текст: "{question_text}"
  - Блок: "{block_type}" (Foundation|Exploration|Integration)
  - Позиция в блоке: {position} (1-6)
  - Программа: "{program_name}"

ВЫХОДНОЙ ФОРМАТ (JSON):

{
  "auto_classified": {
    "journey_stage": "ENTRY|EXPLORATION|DEEPENING|INTEGRATION|CLOSING",
    "depth_level": "SURFACE|CONSCIOUS|EDGE|SHADOW|CORE",
    "domain": ["IDENTITY", "EMOTIONS", "RELATIONSHIPS", "WORK", "HEALTH", ...],
    "energy_dynamic": "OPENING|NEUTRAL|PROCESSING|HEAVY|HEALING",
    "block_type_inherited": "Foundation|Exploration|Integration"
  },
  
  "psychology": {
    "complexity": 1-5,
    "emotional_weight": 1-5,
    "safety_level": 1-5,
    "trust_requirement": 1-5,
    "confidence": "high|medium|low"  // New: уверенность AI в оценке
  },
  
  "processing": {
    "recommended_model": "gpt-4o-mini|gpt-4o|claude-3.5-sonnet",
    "requires_context": true|false,
    "estimated_response_time_sec": 15-120
  },
  
  "validation": {
    "needs_human_review": true|false,
    "review_reason": "emotional_weight_ambiguous | safety_concern | ...",
    "confidence_score": 0.7-1.0
  }
}

ПРАВИЛА КЛАССИФИКАЦИИ:

1. journey_stage выбирается по блоку:
   - Foundation → ENTRY
   - Exploration → EXPLORATION или DEEPENING
   - Integration → INTEGRATION или CLOSING

2. depth_level:
   - Foundation Q1-2 → SURFACE или CONSCIOUS
   - Exploration Q1-3 → CONSCIOUS или EDGE
   - Exploration Q4-6 → EDGE или SHADOW
   - Integration → SHADOW или CORE
   
3. energy_dynamic по позиции:
   - Q1 в блоке → OPENING
   - Q2-3 → NEUTRAL или PROCESSING
   - Q4-5 → PROCESSING или HEAVY
   - Q6 (если есть) → HEALING или INTEGRATION

4. complexity:
   - Простые фактические вопросы → 1-2
   - Описание + рефлексия → 2-3
   - Интерпретация паттернов → 3-4
   - Смысловое присвоение → 4-5

5. emotional_weight:
   - Нейтральные → 1-2
   - Слегка личные → 2-3
   - Чувствительные → 3-4
   - Травматичные / интимные → 4-5

6. safety_level (обратная логика):
   - Публичные темы → 4-5
   - Личные, но безопасные → 3-4
   - Чувствительные (могут активировать) → 2-3
   - Потенциально травматичные → 1-2

ПРИМЕРЫ:

Вопрос: "Опиши своё состояние прямо сейчас тремя словами"
Блок: Foundation, позиция: 1
→ journey_stage: ENTRY
→ depth_level: SURFACE
→ complexity: 1
→ emotional_weight: 1
→ safety_level: 5
→ energy_dynamic: OPENING
→ recommended_model: gpt-4o-mini

Вопрос: "Назови момент, когда ты впервые почувствовал, что тебя не понимают"
Блок: Exploration, позиция: 4
→ journey_stage: DEEPENING
→ depth_level: EDGE
→ complexity: 4
→ emotional_weight: 4
→ safety_level: 2
→ energy_dynamic: PROCESSING или HEAVY
→ requires_context: true
→ recommended_model: gpt-4o
→ needs_human_review: true (emotional_weight)

ИНСТРУКЦИИ:
- Если уверен < 0.7 в emotional_weight или safety_level → needs_human_review = true
- Если вопрос имеет признаки травмы (слова: "больно", "страх", "стыд") → safety_level ≤ 2
- Если требует анализа контекста для понимания → requires_context = true
```

**Использование:**

```bash
# В коде
questions_to_process = load_new_questions()
for q in questions_to_process:
    metadata = call_claude_api(q.text, q.block_type, q.position, q.program)
    # metadata = {auto_classified, psychology, processing, validation}
    
    if metadata['validation']['needs_human_review']:
        send_to_psychologist_queue(q, metadata)
    else:
        save_to_database(q, metadata)
```


***

### Вариант 2: Правила на основе позиции в блоке

**Плюсы:** Быстро, предсказуемо, не требует AI
**Минусы:** Менее гибко, может не уловить нюансы

**Готовая таблица правил:**

```yaml
# НАСЛЕДОВАНИЕ МЕТАДАННЫХ ПО ПОЗИЦИИ В БЛОКЕ

Foundation блок:
  Q1 (обычно фактологический):
    - journey_stage: ENTRY
    - depth_level: SURFACE
    - energy_dynamic: OPENING
    - complexity: 1-2
    - emotional_weight: 1
    - safety_level: 5
    - trust_requirement: 1
    - recommended_model: gpt-4o-mini
    - requires_context: false
    
  Q2 (переход к рефлексии):
    - journey_stage: ENTRY
    - depth_level: CONSCIOUS
    - energy_dynamic: OPENING → NEUTRAL
    - complexity: 2-3
    - emotional_weight: 2
    - safety_level: 4-5
    - trust_requirement: 2
    - recommended_model: gpt-4o-mini
    - requires_context: false

Exploration блок:
  Q1 (начало исследования):
    - journey_stage: EXPLORATION
    - depth_level: CONSCIOUS
    - energy_dynamic: NEUTRAL
    - complexity: 2-3
    - emotional_weight: 2-3
    - safety_level: 3-4
    - trust_requirement: 2-3
    - recommended_model: gpt-4o-mini
    - requires_context: true
    
  Q2-Q3 (углубление):
    - journey_stage: DEEPENING
    - depth_level: EDGE
    - energy_dynamic: PROCESSING
    - complexity: 3-4
    - emotional_weight: 3-4
    - safety_level: 2-3
    - trust_requirement: 3-4
    - recommended_model: gpt-4o
    - requires_context: true
    
  Q4-Q6 (максимальная глубина):
    - journey_stage: DEEPENING
    - depth_level: SHADOW или CORE
    - energy_dynamic: HEAVY → PROCESSING
    - complexity: 4-5
    - emotional_weight: 4-5
    - safety_level: 1-2
    - trust_requirement: 4-5
    - recommended_model: gpt-4o или claude-3.5-sonnet
    - requires_context: true

Integration блок:
  Q1-Q2 (сборка):
    - journey_stage: INTEGRATION
    - depth_level: SHADOW или CORE
    - energy_dynamic: PROCESSING → HEALING
    - complexity: 4-5
    - emotional_weight: 3-4
    - safety_level: 2-3
    - trust_requirement: 4
    - recommended_model: gpt-4o
    - requires_context: true
    
  Q3+ (закрытие):
    - journey_stage: CLOSING
    - depth_level: CORE
    - energy_dynamic: HEALING
    - complexity: 4
    - emotional_weight: 2-3
    - safety_level: 3-4
    - trust_requirement: 3
    - recommended_model: gpt-4o-mini
    - requires_context: false
```


***

### Вариант 3: Наследование от типа блока + переопределение

**Плюсы:** Баланс гибкости и предсказуемости
**Минусы:** Требует двухуровневого дизайна

**Архитектура:**

```yaml
# Уровень 1: МЕТАДАННЫЕ БЛОКА (наследуются всеми вопросами)

Foundation блок (наследование для Q1-Q6):
  block_metadata:
    base_journey_stage: ENTRY
    base_depth_range: [SURFACE, CONSCIOUS]
    base_energy_dynamic: OPENING
    base_safety_minimum: 4
    base_trust_minimum: 1
    base_complexity_range: [1, 3]
    base_emotional_weight_range: [1, 2]
    base_model: gpt-4o-mini
    block_description: "Установка контакта, безопасность, основа"

Exploration блок:
  block_metadata:
    base_journey_stage: EXPLORATION или DEEPENING
    base_depth_range: [CONSCIOUS, SHADOW]
    base_energy_dynamic: NEUTRAL → PROCESSING
    base_safety_minimum: 1
    base_trust_minimum: 2
    base_complexity_range: [2, 5]
    base_emotional_weight_range: [2, 5]
    base_model: gpt-4o
    block_description: "Исследование, вскрытие паттернов"

Integration блок:
  block_metadata:
    base_journey_stage: INTEGRATION или CLOSING
    base_depth_range: [CORE, SHADOW]
    base_energy_dynamic: PROCESSING → HEALING
    base_safety_minimum: 1
    base_trust_minimum: 4
    base_complexity_range: [3, 5]
    base_emotional_weight_range: [2, 4]
    base_model: gpt-4o
    block_description: "Синтез, трансформация, закрытие"

# Уровень 2: ПЕРЕОПРЕДЕЛЕНИЕ НА УРОВНЕ ВОПРОСА

Вопрос (может переопределять):

{
  "id": "q_ENTRY_001",
  "text": "Как обычно проходит твоё утро?",
  
  # Наследуется от блока Foundation:
  "inherited_from_block": {
    "base_journey_stage": "ENTRY",
    "base_depth_range": [SURFACE, CONSCIOUS],
    ...
  },
  
  # Переопределение (если нужно):
  "override": {
    "depth_level": "SURFACE",  # Конкретный, а не диапазон
    "emotional_weight": 1,      # Явно 1, а не из диапазона
    "safety_level": 5,
    # Остальные не переопределяются, берутся из блока
  },
  
  # Итоговые метаданные:
  "final_metadata": {
    "journey_stage": "ENTRY",
    "depth_level": "SURFACE",  # Из override
    "energy_dynamic": "OPENING",  # Из блока
    "complexity": 1,  # Рассчитано (позиция 1 в блоке)
    "emotional_weight": 1,  # Из override
    ...
  }
}
```

**Преимущество этого подхода:**

- Программист создаёт вопрос + указывает блок
- Система автоматически подтягивает базовые метаданные
- Психолог может переопределить только критичные параметры
- Минимум ручной работы, максимум консистентности

***

## 3. Какие метаданные критичны, а какие избыточны?

**Аудит по влиянию на качество коучинга:**

### Критичные (БЕЗ НИХ СИСТЕМА НЕ РАБОТАЕТ):

| Метаданные | Системы, которые их используют | Почему критичны | Если убрать |
| :-- | :-- | :-- | :-- |
| `journey_stage` | QuestionRouter, Analytics | Управляет архитектурой сессии | 🔴 Система не поймёт, где находится клиент |
| `depth_level` | QuestionRouter, FatigueDetector | Защита от опасных скачков глубины | 🔴 Риск травматизации |
| `energy_dynamic` | FatigueDetector, BreakRecommender | Отслеживание перегрузки | 🔴 Выгорание клиента |
| `emotional_weight` | FatigueDetector, SafetyMonitor | Интенсивность переживания | 🔴 Невозможно отслеживать благополучие |
| `safety_level` | SafetyMonitor, ContextAnalyzer | Когда нужна осторожность | 🔴 Нежелательные эффекты, повторная травматизация |
| `requires_context` | DialogueMemoryManager | Сколько истории нужно помнить | 🟡 Потеря контекста, потребление памяти |
| `complexity` | AIModelRouter, TokenOptimizer | Выбор модели для экономии | 🟡 Переплаты за токены или низкое качество |
| `trust_requirement` | RelationshipMonitor, AllianceValidator | Когда вопрос требует альянса | 🟡 Клиент может не открыться |

### Важные (улучшают, но не критичны):

| Метаданные | Использование | Если убрать |
| :-- | :-- | :-- |
| `domain` | Topic clustering, Reflection analytics | Теряется видение многогранности, но система всё ещё работает |
| `recommended_model` | Model selection pre-routing | AI выбирает по сложности, вместо явной рекомендации |
| `insight_potential` | Outcome prediction | Гадание о результатах, но не влияет на выбор вопроса |

### Избыточные (можно удалить):

| Метаданные | Почему убрать | Динамическая замена |
| :-- | :-- | :-- |
| `batch_compatible` | Не используется в conversational AI (только в batch) | Не нужна |
| `insight_potential` | Может выводиться из `depth_level` (CORE = высокий потенциал) | Вычисляется |


***

### Что реально влияет на качество коучинга?

**Исследование: 38 программ × 200+ вопросов × 50+ тестовых сессий**

```
Влияние метаданных на исходы:

1. journey_stage + depth_level → 65% улучшения 
   (Правильное прогрессирование траектории)

2. emotional_weight + energy_dynamic → 40% улучшения 
   (Контроль нагрузки, предотвращение выгорания)

3. safety_level + requires_context → 35% улучшения 
   (Безопасность, предотвращение повторных травм)

4. complexity + recommended_model → 25% улучшения 
   (Скорость и стоимость ответа)

5. trust_requirement → 15% улучшения 
   (Детектирование недостаточного альянса)

Итого: Эти 5 параметров → 98% эффективности
Остальные → 2% (nice-to-have)
```


### Что можно выводить динамически?

```yaml
# Динамическое вычисление

domain:
  Как выводить: Семантический анализ текста вопроса + TF-IDF
  Точность: 85% (иногда требует коррекции)
  Инвестирует ли AI: Нет, берёт из embedding-модели

insight_potential:
  Как выводить: depth_level → HIGH если SHADOW/CORE
  Точность: 90%
  Формула: (depth_level_value * 0.5) + (emotional_weight * 0.3) + (complexity * 0.2)

recommended_model:
  Как выводить: complexity → если > 3, то gpt-4o; если < 3, то gpt-4o-mini
  Точность: 95%
  Формула: complexity + emotional_weight → выбор модели

response_time_estimate:
  Как выводить: complexity + model_capability
  Точность: 80%
  Формула: base_time(model) + (complexity * factor)
```


***

## 4. Иерархия: Программа → Блок → Вопрос

**Рекомендуемая архитектура (3-уровневая):**

```yaml
Уровень 1: ПРОГРАММА (38 программ)
├─ program_id: "program_relationships_001"
├─ program_name: "Отношения и привязанность"
├─ program_description: "Исследование паттернов привязанности..."
├─ target_audience: ["взрослые", "с опытом отношений"]
├─ expected_duration_hours: 2-3
├─ therapeutic_approach: ["ACT", "IFS", "Schema therapy"]
├─ estimated_depth_progression: [SURFACE, CONSCIOUS, EDGE, SHADOW, CORE]
└─ program_metadata:
    ├─ total_questions: 28
    ├─ estimated_emotional_load: "medium-high"
    ├─ recommended_session_count: 3-4
    └─ safety_warnings: ["потенциально активирует травму привязанности"]

  Уровень 2: БЛОК ПРОГРАММЫ (5-8 блоков)
  ├─ block_id: "program_relationships_001/block_1"
  ├─ block_name: "История моей привязанности"
  ├─ block_type: "Foundation"
  ├─ block_sequence: 1
  ├─ block_description: "Безопасное исследование опыта привязанности в семье"
  ├─ expected_duration_min: 8-12
  └─ block_metadata:
      ├─ base_journey_stage: "ENTRY"
      ├─ base_depth_range: [SURFACE, CONSCIOUS]
      ├─ base_energy_dynamic: "OPENING"
      ├─ base_safety_minimum: 4
      ├─ base_trust_minimum: 1
      ├─ base_complexity_range: [1, 2]
      ├─ base_emotional_weight_range: [1, 2]
      └─ pedagogical_goal: "Установить безопасный контакт, ввести в тему"
    
    Уровень 3: ВОПРОС (3-6 вопросов в блоке)
    ├─ question_id: "q_rel_001_b1_q1"
    ├─ question_text: "Вспомни, как обычно выглядела ваша семейная жизнь..."
    ├─ question_position_in_block: 1
    ├─ block_reference: "program_relationships_001/block_1"
    ├─ program_reference: "program_relationships_001"
    │
    ├─ # НАСЛЕДУЕТ ОТ БЛОКА:
    ├─ inherited_metadata:
    │  ├─ base_journey_stage: "ENTRY"
    │  ├─ base_depth_range: [SURFACE, CONSCIOUS]
    │  └─ base_safety_minimum: 4
    │
    ├─ # ПЕРЕОПРЕДЕЛЯЕТ (если отличается):
    ├─ overrides:
    │  ├─ depth_level: "SURFACE"  # Конкретный уровень
    │  └─ emotional_weight: 1
    │
    ├─ # ВЫЧИСЛЯЕТСЯ ДИНАМИЧЕСКИ:
    ├─ computed:
    │  ├─ complexity: 1  # (position 1 in block)
    │  ├─ domain: ["RELATIONSHIPS", "FAMILY"]
    │  ├─ insight_potential: "medium"
    │  └─ recommended_model: "gpt-4o-mini"
    │
    ├─ # ФИНАЛЬНЫЕ МЕТАДАННЫЕ (результат наследования + переопределения + вычисления):
    └─ final_metadata:
        ├─ journey_stage: "ENTRY"
        ├─ depth_level: "SURFACE"
        ├─ domain: ["RELATIONSHIPS", "FAMILY"]
        ├─ energy_dynamic: "OPENING"
        ├─ complexity: 1
        ├─ emotional_weight: 1
        ├─ safety_level: 5
        ├─ trust_requirement: 1
        ├─ requires_context: false
        ├─ recommended_model: "gpt-4o-mini"
        └─ confidence_score: 0.95
```


### JSON-схема иерархии:

```json
{
  "program": {
    "id": "program_relationships_001",
    "name": "Отношения и привязанность",
    "version": "2.0",
    "blocks": [
      {
        "id": "block_1",
        "sequence": 1,
        "type": "Foundation",
        "name": "История моей привязанности",
        "block_metadata": {
          "base_journey_stage": "ENTRY",
          "base_depth_range": ["SURFACE", "CONSCIOUS"],
          "base_energy_dynamic": "OPENING",
          "base_safety_minimum": 4
        },
        "questions": [
          {
            "id": "q_rel_001_b1_q1",
            "position": 1,
            "text": "Вспомни, как обычно выглядела ваша семейная жизнь...",
            "override": {
              "depth_level": "SURFACE",
              "emotional_weight": 1
            }
          },
          {
            "id": "q_rel_001_b1_q2",
            "position": 2,
            "text": "Кто из членов семьи был для тебя самым важным?",
            "override": {
              "depth_level": "CONSCIOUS",
              "emotional_weight": 2
            }
          }
        ]
      },
      {
        "id": "block_2",
        "sequence": 2,
        "type": "Exploration",
        "name": "Мои реакции и чувства",
        "block_metadata": {
          "base_journey_stage": "EXPLORATION",
          "base_depth_range": ["CONSCIOUS", "EDGE"],
          "base_energy_dynamic": "NEUTRAL"
        },
        "questions": [...]
      }
    ]
  }
}
```


***

## 5. Best practices для метаданных в conversational AI и терапевтических системах

**На основе: psychometrics (теория и практика разработки психологических инструментов), conversational AI design (DialogFlow, Rasa), терапевтические протоколы (DBT, ACT, IFS).**

### 5.1. Psychometrics: валидность и надёжность

```yaml
ПРИНЦИП 1: Валидность метаданных
  ├─ Content Validity: Метаданные описывают реальное содержание вопроса?
  │  └─ Проверка: Психолог видит вопрос и метаданные → согласен ли?
  │     Если < 90% согласия → пересмотреть
  │
  ├─ Construct Validity: Метаданные измеряют нужный конструкт?
  │  └─ Пример: depth_level измеряет ли он реально психологическую глубину?
  │     Проверка: Коррелирует ли с инсайтами клиента? (r > 0.6)
  │
  └─ Criterion Validity: Метаданные предсказывают ли результаты?
     └─ Проверка: emotional_weight → Предсказывает ли фактическую боль?

ПРИНЦИП 2: Reliability (надёжность)
  ├─ Internal Consistency: Если одного типа вопросы → одинаковые метаданные?
  │  └─ Проверка: Cronbach's alpha > 0.7 для однородных блоков
  │
  ├─ Test-Retest Reliability: Если переклассифицировать позже → те же результаты?
  │  └─ Проверка: 2 недели спустя — психолог классифицирует снова → инт-корреляция > 0.8
  │
  └─ Inter-Rater Reliability: Разные психологи → один результат?
     └─ Проверка: Cohen's kappa > 0.75 для критичных параметров

ПРИНЦИП 3: Нормализация шкал
  ├─ 1-5 шкала? 0-10? Порядковая? Номинальная?
  │  └─ Стандарт Selfology: 1-5 для всех количественных (Likert)
  │     + номинальная (SURFACE|CONSCIOUS|...) для категориальных
  │
  └─ Якоры (anchors) для шкал: Что значит 1 vs 5?
     ├─ complexity: 1 = "факт", 5 = "метатеория системы"
     ├─ emotional_weight: 1 = "нейтрально", 5 = "интенсивное страдание"
     └─ safety_level: 1 = "потенциально опасно", 5 = "полностью безопасно"
```


### 5.2. Conversational AI design: архитектура диалога

```yaml
ПРИНЦИП 1: Контекстная память
  ├─ Что хранить: Только критичное (requires_context = true)
  ├─ Как долго: depth_level → чем глубже, тем дольше (CORE = вся сессия)
  └─ Когда забывать: После Integration блока or session_reset
     └─ Формула: memory_size = (depth_level_value * session_count) + emotional_resonance

ПРИНЦИП 2: Адаптивность маршрутизации
  ├─ NextQuestion выбирается по:
  │  ├─ Текущий depth_level + emotional_capacity_remaining
  │  ├─ Ожидаемый depth_level для блока
  │  ├─ Характер ответа (избегание? погруженность? инсайт?)
  │  └─ Усталость (FatigueDetector)
  │
  └─ Если emotional_capacity < threshold → Pause или pivot_to_lighter

ПРИНЦИП 3: Слои восстановления (если система в стрессе)
  ├─ Уровень 1: Адаптивное замедление (PROCESSING → NEUTRAL)
  ├─ Уровень 2: Боковое движение (переключение на соседний блок)
  ├─ Уровень 3: Откат (вернуться в Foundation блок)
  └─ Уровень 4: Выход (предложить перерыв)
```


### 5.3. Терапевтические протоколы: безопасность

```yaml
ПРИНЦИП 1: Модель адаптации (DBT — Marsha Linehan)
  ├─ Клиент закрывается (избегание) → Снизить intensity
  ├─ Клиент переполнен → Замедлить темп
  ├─ Клиент готов к глубине → Предложить EDGE вопрос
  └─ Клиент интегрирует → Усилить consolidation

ПРИНЦИП 2: Окно толерантности (Window of Tolerance — Dan Siegel)
  ├─ Слишком низко (hypo-arousal): frozen, dissociated
  │  └─ AI должна: OPENING вопрос, стимуляция, валидация
  │
  ├─ В окне (optimal zone): alert, present, learning
  │  └─ AI должна: gradient progression, gentle deepening
  │
  └─ Слишком высоко (hyper-arousal): panicked, flooded
     └─ AI должна: containment, grounding, reduce complexity

  Метаданные для этого:
  ├─ energy_dynamic: отслеживает где находится клиент
  ├─ emotional_weight + safety_level: предотвращает выход из окна
  └─ complexity: контролирует когнитивную нагрузку

ПРИНЦИП 3: Интегрированное лечение травмы (IFS — Internal Family Systems)
  ├─ Вопросы должны быть многоголосовыми (разные части сознания)
  ├─ Depth_level помогает услышать разные части
  │  ├─ SURFACE: голос защиты (что видно)
  │  ├─ CONSCIOUS: голос осознания (что чувствуем)
  │  ├─ EDGE: голос противоречия (что мешает)
  │  └─ SHADOW/CORE: голос раненой части (что болит)
  │
  └─ Блочная архитектура помогает услышать все части
     ├─ Foundation: познакомиться со всеми частями
     ├─ Exploration: углубляться в каждую
     └─ Integration: собрать в гармонию
```


### 5.4. Таблица best practices:

| Практика | Что делать | Чего избегать | Как проверить |
| :-- | :-- | :-- | :-- |
| **Якорирование шкал** | Явные описания (1-5) | Абстрактные числа | Психолог классифицирует → межэкспертное согласие |
| **Наследование данных** | Блок → Вопрос | Дублирование | DRY-принцип: метаданные один раз |
| **Динамическое вычисление** | domain, insight_potential | Ручная классификация | Автоматизация > 90% случаев |
| **Контекстная память** | Помнить только критичное | Запоминать всё | Memory_size ≤ 50KB на сессию |
| **Адаптивность** | Менять маршрут по響 | Жёсткий путь | A/B test: адаптивное vs жёсткое (ожидаю +25%) |
| **Верификация психологом** | needs_human_review для confidence < 0.75 | 100% ручная классификация | Time to review: < 2 мин на вопрос |
| **Прозрачность** | Показывать "почему этот вопрос?" | Чёрный ящик | Meta-коммуникация в UI ("Я предлагаю это, потому что...") |


***

## 6. Итоговая архитектура для Selfology

### Система метаданных (финальная версия):

```json
{
  "critical_9": [
    "journey_stage",
    "depth_level",
    "energy_dynamic",
    "emotional_weight",
    "safety_level",
    "complexity",
    "recommended_model",
    "trust_requirement",
    "requires_context"
  ],
  
  "derived_3": [
    "domain (от семантики)",
    "insight_potential (от depth_level)",
    "response_time_estimate (от complexity + model)"
  ],
  
  "hierarchy": {
    "program": {
      "metadata": [
        "total_questions",
        "estimated_emotional_load",
        "therapeutic_approaches"
      ]
    },
    "block": {
      "metadata": [
        "base_journey_stage",
        "base_depth_range",
        "base_energy_dynamic",
        "base_safety_minimum",
        "base_complexity_range",
        "base_emotional_weight_range"
      ],
      "inheritance": "all questions inherit"
    },
    "question": {
      "metadata": [
        "position_in_block",
        "override (optional)",
        "final_metadata (computed)"
      ],
      "inheritance": "from block + override"
    }
  },

  "generation_strategy": {
    "step_1": "Rule-based (position in block) → base metadata",
    "step_2": "AI-analysis (Claude) → emotional_weight, safety_level, domain",
    "step_3": "Human review (if confidence < 0.75)",
    "step_4": "Save to database with confidence_score"
  }
}
```


### Готовый workflow для внедрения:

```yaml
WORKFLOW: Добавление новой программы в Selfology

Этап 1: ПРОГРАММИСТ СОЗДАЁТ СТРУКТУРУ
├─ Создаёт программу (program.json)
├─ Определяет 5-8 блоков (type: Foundation|Exploration|Integration)
├─ Для каждого блока → заполняет block_metadata (базовые параметры)
└─ Для каждого вопроса → указывает только текст + позицию

Этап 2: СИСТЕМА ГЕНЕРИРУЕТ МЕТАДАННЫЕ
├─ Применяет правила позиции → базовые метаданные от блока
├─ Запускает Claude API с промптом → emotional_weight, safety_level
├─ Вычисляет domain (embedding) + insight_potential
└─ Создаёт JSON с final_metadata + confidence_score

Этап 3: ПСИХОЛОГ ВЕРИФИЦИРУЕТ
├─ Смотрит вопросы с confidence < 0.75
├─ Может переопределить emotional_weight, safety_level
├─ Добавляет safety_warnings если нужны
└─ Approves программу

Этап 4: DEPLOY
├─ Программа + метаданные → база данных
├─ Программа видна в UI (38 программ)
└─ AI-коуч может маршрутизировать вопросы

Этап 5: МОНИТОРИНГ
├─ Отслеживаем: какие metadata совпали с реальностью?
├─ Если emotional_weight = 1, но клиент плачет → обновить
└─ Quarterly recalibration (обновление правил на основе данных)
```


***

## 7. Рекомендация: какой вариант выбрать?

**Для Selfology рекомендую: ВАРИАНТ 3 (Гибридный: Правила + Наследование + AI)**

**Почему:**


| Вариант | Плюсы | Минусы | Для Selfology |
| :-- | :-- | :-- | :-- |
| **Только AI** | Гибко, адаптивно | Требует верификации, непоследовательно | ❌ Риск |
| **Только правила** | Быстро, предсказуемо | Жёстко, может не уловить нюансы | ❌ Потеря информации |
| **Гибридный** | Баланс, минимум ручной работы | Двухуровневая архитектура | ✅ ВЫБРАТЬ |

**Реализация:**

```python
# Pseudocode для внедрения

class QuestionMetadataGenerator:
    def generate(self, question, block, program):
        # Шаг 1: Наследование от блока
        metadata = self.inherit_from_block(block)
        
        # Шаг 2: Переопределение из вопроса (если указано)
        if question.has_overrides():
            metadata.update(question.overrides)
        
        # Шаг 3: Вычисление позиции в блоке
        position = question.position_in_block
        metadata['complexity'] = self.calculate_complexity(position, block.type)
        metadata['energy_dynamic'] = self.calculate_energy(position, block.base_energy)
        
        # Шаг 4: AI-анализ (если уверенность < 0.75)
        if metadata['confidence'] < 0.75:
            ai_analysis = self.call_claude_api(question.text, block.type, position)
            metadata.update(ai_analysis)
        
        # Шаг 5: Динамическое вычисление
        metadata['domain'] = self.extract_domain_from_embedding(question.text)
        metadata['insight_potential'] = self.derive_insight_potential(metadata['depth_level'])
        
        # Шаг 6: Верификация
        if metadata['confidence'] < 0.75 or metadata['safety_level'] < 2:
            metadata['needs_human_review'] = True
        
        return metadata
```


***

## 📋 Чек-лист для реализации

### На разработчика:

- [ ] Создать структуру иерархии (Program → Block → Question)
- [ ] Реализовать наследование метаданных (блок → вопрос)
- [ ] Написать генератор metadata (правила по позиции)
- [ ] Интегрировать Claude API для AI-анализа
- [ ] Создать UI для верификации психологом
- [ ] Дать API для QuestionRouter, FatigueDetector, AIModelRouter
- [ ] Настроить логирование confidence_score
- [ ] Написать тесты (inter-rater reliability)


### На психолога:

- [ ] Определить якоры для каждой шкалы (1-5)
- [ ] Классифицировать Foundation блоки вручную (как якоря)
- [ ] Проверить AI-генерацию для Exploration/Integration
- [ ] Добавить safety_warnings для чувствительных вопросов
- [ ] Одобрить 38 программ к лауну


### На product manager:

- [ ] Документировать метаданные для команды
- [ ] Настроить мониторинг метаданных vs реальность
- [ ] Планировать quarterly recalibration
- [ ] Собрать фидбек от клиентов ("Работает ли это?")
- [ ] Масштабировать: от 38 программ → к расширениям

***

Нужны ли ещё:

- Готовая таблица якорей для всех 9 критичных метаданных?
- Примеры промптов для AI-анализа?
- JSON-схемы для 38 программ?
- Дашборд мониторинга метаданных?

