# 📂 Полная структура проекта Selfology

> **Объяснение на человеческом языке для подростков**
>
> Этот документ описывает как организован код проекта Selfology - Telegram бота для самопознания и психологического анализа личности.

## Оглавление
1. [Структура директорий](#структура-директорий)
2. [Карта связей между системами](#карта-связей-между-системами)
3. [Event Flow (поток событий)](#event-flow-как-события-идут-через-систему)
4. [Blue-Green Deployment](#blue-green-deployment-structure)
5. [Размер кода](#размер-кода-lines-of-code)
6. [Аналогия здания](#как-это-всё-работает-вместе-простыми-словами)

---

## Структура директорий

```
/home/ksnk/n8n-enterprise/projects/selfology/
│
├── 🏗️ systems/                          # ОСНОВНЫЕ МИКРОСЕРВИСЫ (11 файлов)
│   ├── base.py                          # Базовый класс для всех сервисов
│   ├── analysis_system.py               # Старая версия (deprecated)
│   │
│   ├── 📊 analysis/                     # СИСТЕМА АНАЛИЗА (Phase 2)
│   │   ├── analysis_worker_service.py   # Главный воркер: instant + deep анализ (686 строк)
│   │   └── ai_router.py                 # Умный роутер AI моделей с Circuit Breaker (531 строка)
│   │
│   ├── 👤 profile/                      # СИСТЕМА ПРОФИЛЕЙ (Phase 3)
│   │   ├── profile_storage_service.py   # Хранение профилей в PostgreSQL + Qdrant (560 строк)
│   │   └── trait_evolution_service.py   # Отслеживание изменений черт личности (647 строк)
│   │
│   ├── 🎯 onboarding/                   # СИСТЕМА ОНБОРДИНГА (Phase 1)
│   │   ├── question_selection_service.py # Умный выбор вопросов (559 строк)
│   │   └── session_management_service.py # Управление сессиями FSM (585 строк)
│   │
│   ├── 💬 telegram/                     # TELEGRAM GATEWAY (Phase 4)
│   │   └── telegram_gateway_service.py  # Чистый event-driven шлюз (651 строка)
│   │
│   └── 🧠 coach/                        # КОУЧИНГ СИСТЕМА (Phase 5)
│       ├── coach_interaction_service.py # Персонализированные советы (560 строк)
│       └── insight_generator_service.py # Генерация инсайтов (163 строки)
│
├── 🧪 tests/                            # ТЕСТИРОВАНИЕ (Phase 6-7)
│   ├── unit/                            # Юнит-тесты (8 файлов)
│   │   ├── test_ai_router.py            # Тесты AI Router (407 строк)
│   │   ├── test_circuit_breaker.py      # Тесты Circuit Breaker
│   │   ├── test_profile_storage_service.py  # (600 строк)
│   │   ├── test_trait_evolution_service.py  # (588 строк)
│   │   ├── test_question_selection_service.py
│   │   ├── test_session_management_service.py
│   │   ├── test_telegram_gateway_service.py # (500 строк)
│   │   └── test_retry.py
│   │
│   ├── integration/                     # Интеграционные тесты
│   │   └── test_event_bus_with_outbox.py # Тесты Outbox Pattern
│   │
│   ├── e2e/                             # End-to-End тесты (Phase 6)
│   │   ├── test_onboarding_flow.py      # Тест полного онбординга
│   │   └── test_complete_user_journey.py # Полный путь пользователя (407 строк)
│   │
│   ├── load/                            # Нагрузочное тестирование (Phase 7)
│   │   └── k6_load_test.js              # K6 скрипты: smoke/load/stress/spike (361 строка)
│   │
│   ├── chaos/                           # Chaos Engineering (Phase 7)
│   │   └── chaos_engineering_tests.py   # 6 сценариев хаоса (677 строк)
│   │
│   └── performance/                     # Профилирование (Phase 7)
│       └── profiling_tests.py           # CPU/Memory/Timing тесты (679 строк)
│
├── 🚀 deployment/                       # PRODUCTION DEPLOYMENT (Phase 8)
│   ├── docker-compose.production.yml    # Blue-Green слоты (442 строки)
│   ├── blue-green-deploy.sh             # Скрипт деплоя (443 строки)
│   ├── zero-downtime-migration.py       # Миграция без даунтайма (636 строк)
│   │
│   └── monitoring/                      # Мониторинг
│       ├── prometheus.yml               # Конфиг Prometheus (207 строк)
│       ├── alerts.yml                   # 25 алертов в 10 группах (311 строк)
│       ├── alertmanager.yml             # Конфиг Alertmanager (130 строк)
│       │
│       └── grafana/provisioning/        # Grafana dashboards
│           ├── datasources/prometheus.yml
│           └── dashboards/default.yml
│
├── 📖 docs/                             # ДОКУМЕНТАЦИЯ (Phase 9)
│   ├── API_DOCUMENTATION.md             # API референс всех сервисов (674 строки)
│   ├── ARCHITECTURE.md                  # Архитектура системы (749 строк)
│   ├── RUNBOOK.md                       # Operational playbook (744 строки)
│   └── PROJECT_STRUCTURE.md             # Этот файл - структура проекта
│
├── 🗄️ selfology_bot/                   # LEGACY МОНОЛИТ (старая версия)
│   ├── services/                        # Старые сервисы (будут deprecated)
│   │   ├── onboarding/
│   │   │   ├── orchestrator.py          # Старый оркестратор (719 строк - REFACTORED!)
│   │   │   ├── question_router.py
│   │   │   └── fatigue_detector.py
│   │   ├── user_service.py
│   │   ├── memory_system.py
│   │   └── vector_service.py
│   │
│   └── soul_architect/                  # Психологический анализ
│       ├── migrations/                  # Alembic миграции
│       │   └── versions/
│       │       └── 001_create_soul_architect_tables.py  # 3 таблицы профилей
│       └── tests/
│
├── 📊 database/                         # БАЗА ДАННЫХ
│   └── migrations/                      # SQL миграции
│       └── alembic/
│
└── 🗂️ archive/                          # Архив старого кода
    └── deprecated_services/
        └── intelligent_questioning.py
```

---

## Карта связей между системами

```
┌─────────────────────────────────────────────────────────────────────┐
│                        TELEGRAM BOT (пользователь)                   │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
        ┌────────────────────────────────────────────────┐
        │     🚪 Telegram Gateway Service                │
        │     systems/telegram/telegram_gateway_service.py│
        │                                                 │
        │  • Rate Limiter (Redis)                        │
        │  • Event Publisher                             │
        │  • Message Handler                             │
        └────────────────┬───────────────────────────────┘
                         │
                         │ публикует события
                         ▼
        ┌────────────────────────────────────────────────┐
        │         📢 REDIS STREAMS (Event Bus)           │
        │                                                 │
        │  • user.onboarding.started                     │
        │  • answer.received                             │
        │  • question.selected                           │
        │  • analysis.instant.completed                  │
        │  • analysis.deep.completed                     │
        │  • profile.updated                             │
        └────┬────────┬────────┬────────┬────────┬───────┘
             │        │        │        │        │
   ┌─────────┘        │        │        │        └──────────┐
   │                  │        │        │                   │
   ▼                  ▼        ▼        ▼                   ▼
┌──────────┐    ┌──────────┐ ┌────────┐ ┌──────────┐  ┌────────┐
│🎯Onboard-│    │📊Analysis│ │👤Profile│ │🧠Coach   │  │ Outbox │
│  ing     │    │  Worker  │ │ Storage│ │Interac-  │  │Worker  │
│          │    │          │ │        │ │tion      │  │        │
│• Question│    │• Instant │ │• Deep  │ │• Context │  │• Retry │
│Selection │    │  <500ms  │ │  Merge │ │  Loading │  │• Resend│
│• Session │    │• Deep AI │ │• Qdrant│ │• Insight │  │        │
│  FSM     │    │  2-10s   │ │  Save  │ │  Gen     │  │        │
└────┬─────┘    └────┬─────┘ └───┬────┘ └────┬─────┘  └────────┘
     │               │           │           │
     └───────┬───────┴───────────┴───────────┘
             │
             ▼
   ┌─────────────────────────────────────────┐
   │      🗄️ POSTGRESQL (selfology schema)  │
   │                                          │
   │  • users                                 │
   │  • questions                             │
   │  • user_answers                          │
   │  • answer_analysis                       │
   │  • personality_profiles (Big Five)       │
   │  • trait_history (изменения черт)        │
   │  • user_progress                         │
   │  • conversation_context                  │
   │  • outbox (гарантированная доставка)     │
   └──────────────────────────────────────────┘

   ┌─────────────────────────────────────────┐
   │      🔥 REDIS (кэш + FSM + rate limit)  │
   │                                          │
   │  • fsm:user:{id} → состояние            │
   │  • cache:profile:{id} → кэш профиля     │
   │  • rate_limit:user:{id} → счётчик       │
   └──────────────────────────────────────────┘

   ┌─────────────────────────────────────────┐
   │      🧮 QDRANT (векторная база)         │
   │                                          │
   │  • selfology_answers (коллекция)        │
   │    - vectors[1536] (эмбеддинги)         │
   │    - metadata (user_id, question_id)    │
   │    - payload (ответ, sentiment, traits) │
   └──────────────────────────────────────────┘
```

---

## Event Flow (как события идут через систему)

```
1️⃣  USER → /start
         ↓
2️⃣  Telegram Gateway
         ↓ публикует: user.onboarding.started
         ↓
3️⃣  Question Selection Service слушает событие
         ↓ читает: вопросы из PostgreSQL
         ↓ публикует: question.selected {question_text}
         ↓
4️⃣  Telegram Gateway получает question.selected
         ↓ отправляет вопрос пользователю
         ↓
5️⃣  USER → "Я люблю программирование"
         ↓
6️⃣  Telegram Gateway
         ↓ сохраняет ответ в user_answers (PostgreSQL)
         ↓ публикует: answer.received {answer_text}
         ↓
         ├──────────────────────────────────────┐
         │                                      │
7️⃣-A    ▼ БЫСТРО                          7️⃣-B ▼ МЕДЛЕННО (параллельно)
    Analysis Worker                           Analysis Worker
         ↓ GPT-4o-mini                             ↓ Claude/GPT-4o
         ↓ instant анализ <500ms                   ↓ deep анализ 2-10s
         ↓ публикует: analysis.instant.completed   ↓ публикует: analysis.deep.completed
         ↓                                          ↓
8️⃣-A    ▼                                      8️⃣-B ▼
    Telegram Gateway                          Profile Storage Service
         ↓ отправляет: "Интересно! 🤔"             ↓ deep merge профиля
         ↓                                          ↓ сохраняет в PostgreSQL
         ↓                                          ↓ сохраняет embedding в Qdrant
         ↓                                          ↓ публикует: profile.updated
         │                                          ↓
         │                                      9️⃣  ▼
         │                                      Trait Evolution Service
         │                                          ↓ детектирует паттерны
         │                                          ↓ сохраняет в trait_history
         │                                          ↓
         └──────────────────────────────────────────┘
                              │
                          🔁 повтор для следующего вопроса
```

**Объяснение для подростков:**

1. Ты пишешь `/start` в Telegram
2. Telegram Gateway получает твоё сообщение и кричит в Redis: "Новый пользователь!"
3. Question Selection Service слышит это и достаёт первый вопрос из базы
4. Telegram Gateway отправляет тебе вопрос
5. Ты отвечаешь: "Я люблю программирование"
6. Gateway сохраняет твой ответ и снова кричит в Redis: "Новый ответ!"
7. **МАГИЯ**: Два воркера работают ОДНОВРЕМЕННО:
   - **Быстрый**: за 300 миллисекунд говорит "Интересно! 🤔" (ты НЕ ждёшь!)
   - **Медленный**: за 5 секунд делает глубокий анализ твоей личности
8. Быстрый воркер сразу отправляет тебе реакцию
9. Медленный воркер обновляет твой профиль в базе данных
10. Trait Evolution Service смотрит: "О, у Саши Openness выросла с 0.6 до 0.8!"
11. И так повторяется для каждого вопроса

---

## Blue-Green Deployment Structure

```
deployment/
│
├── docker-compose.production.yml
│   │
│   ├── 🔵 BLUE SLOT (активный)
│   │   ├── analysis-worker-blue:8000
│   │   ├── profile-storage-blue:8001
│   │   ├── question-selection-blue:8002
│   │   ├── session-management-blue:8003
│   │   ├── telegram-gateway-blue:8004
│   │   ├── coach-interaction-blue:8005
│   │   └── trait-evolution-blue:8006
│   │
│   └── 🟢 GREEN SLOT (резервный, для deploy)
│       ├── analysis-worker-green:9000
│       ├── profile-storage-green:9001
│       ├── question-selection-green:9002
│       ├── session-management-green:9003
│       ├── telegram-gateway-green:9004
│       ├── coach-interaction-green:9005
│       └── trait-evolution-green:9006
│
├── 📊 MONITORING STACK
│   ├── prometheus:9090 → собирает метрики с blue + green
│   ├── grafana:3000 → дашборды
│   └── alertmanager:9093 → алерты в Telegram
│
└── 🗄️ SHARED DATA LAYER
    ├── postgres:5432 (оба слота используют одну БД)
    ├── redis:6379 (общий кэш + event bus)
    └── qdrant:6333 (векторная база)
```

**Как работает Blue-Green:**

Представь, что у тебя два одинаковых сервера:
- **BLUE** - работает прямо сейчас, обслуживает пользователей
- **GREEN** - спит и ждёт

Когда нужно обновить код:
1. Заливаешь новую версию на GREEN
2. Проверяешь 5 минут: всё работает?
3. Переключаешь пользователей с BLUE на GREEN
4. BLUE теперь спит, GREEN работает
5. Если что-то сломалось - переключаешься обратно за 60 секунд!

**Пользователи НЕ замечают обновления!** Бот работает 24/7 без остановок.

---

## Размер кода (Lines of Code)

```
┌──────────────────────────────────┬────────────┐
│          Категория               │   Строки   │
├──────────────────────────────────┼────────────┤
│ 🏗️  Microservices (systems/)    │   5,242    │
│   ├─ Analysis System             │   1,217    │
│   ├─ Profile System              │   1,207    │
│   ├─ Onboarding System           │   1,144    │
│   ├─ Telegram Gateway            │     651    │
│   └─ Coach System                │     723    │
├──────────────────────────────────┼────────────┤
│ 🧪 Tests (tests/)                │   5,046    │
│   ├─ Unit Tests                  │   2,095    │
│   ├─ E2E Tests                   │     407    │
│   ├─ Load Tests (K6)             │     361    │
│   ├─ Chaos Engineering           │     677    │
│   └─ Performance Profiling       │     679    │
├──────────────────────────────────┼────────────┤
│ 🚀 Deployment (deployment/)      │   2,169    │
│   ├─ Docker Compose              │     442    │
│   ├─ Blue-Green Script           │     443    │
│   ├─ Migration Script            │     636    │
│   └─ Monitoring (Prom+Alerts)    │     648    │
├──────────────────────────────────┼────────────┤
│ 📖 Documentation (docs/)         │   2,167    │
│   ├─ API Docs                    │     674    │
│   ├─ Architecture                │     749    │
│   └─ Runbook                     │     744    │
├──────────────────────────────────┼────────────┤
│ 🗄️  Legacy (selfology_bot/)     │  ~10,000   │
│    (старый монолит, deprecated)  │            │
├──────────────────────────────────┼────────────┤
│ 📊 TOTAL (новая архитектура)     │  24,891    │
└──────────────────────────────────┴────────────┘
```

**Что это значит:**

- **5,242 строк микросервисов** - это как 5 книг по 100 страниц каждая
- **5,046 строк тестов** - почти столько же тестов, сколько и кода! (это хорошо!)
- **2,169 строк деплоймента** - автоматизация запуска в продакшн
- **2,167 строк документации** - объяснения как это всё работает
- **24,891 строк ВСЕГО** - это примерно 500 страниц книги!

---

## Как это всё работает вместе (простыми словами)

**Представь многоэтажное здание:**

```
🏢 SELFOLOGY ЗДАНИЕ

┌─────────────────────────────────────────────┐
│  20 этаж: 📖 ДОКУМЕНТАЦИЯ                   │ ← Как пользоваться всем этим
│            docs/                            │
├─────────────────────────────────────────────┤
│  19 этаж: 🚀 DEPLOYMENT                     │ ← Как запустить в продакшн
│            deployment/                      │
├─────────────────────────────────────────────┤
│  15-18: 🧪 ТЕСТИРОВАНИЕ                     │ ← Проверка что всё работает
│         tests/ (unit/e2e/load/chaos)        │
├─────────────────────────────────────────────┤
│  10-14: 🏗️ МИКРОСЕРВИСЫ (главное!)         │
│         ┌────────────────────────┐          │
│    14   │ 🧠 Coach               │          │ ← Советы
│    13   │ 💬 Telegram Gateway    │          │ ← Вход/выход
│    12   │ 🎯 Onboarding          │          │ ← Вопросы
│    11   │ 👤 Profile             │          │ ← Хранение личности
│    10   │ 📊 Analysis            │          │ ← AI анализ
│         └────────────────────────┘          │
├─────────────────────────────────────────────┤
│  5-9:  📢 ШИНА СОБЫТИЙ                      │ ← Redis Streams
│        (сообщения летают туда-сюда)         │
├─────────────────────────────────────────────┤
│  1-4:  🗄️ ХРАНИЛИЩЕ ДАННЫХ                  │
│    4   🧮 Qdrant (векторы)                  │
│    3   🔥 Redis (кэш, FSM)                  │
│    2   🗄️ PostgreSQL (основная БД)          │
│    1   💾 Disk (Docker volumes)             │
└─────────────────────────────────────────────┘
```

**Лифт между этажами = Redis Streams (события)**

- Когда ты пишешь сообщение → оно едет на 13 этаж (Gateway)
- Gateway отправляет в лифт событие → едет на 10 этаж (Analysis)
- Analysis анализирует → отправляет на 11 этаж (Profile)
- И так далее... **никто никого не ждёт**, все работают параллельно!

**Почему это круто:**

1. **Быстрота**: Ты не ждёшь - мгновенный ответ за 300ms
2. **Надёжность**: Если один этаж (сервис) сломался - остальные работают
3. **Масштабируемость**: Можно добавить 10 копий каждого этажа
4. **Независимость**: Каждый сервис можно обновлять отдельно

---

## Ключевые файлы для изучения

Если ты хочешь понять как работает Selfology, начни с этих файлов:

### 1. Начни с Gateway
```
systems/telegram/telegram_gateway_service.py
```
Это точка входа - здесь всё начинается

### 2. Посмотри как выбираются вопросы
```
systems/onboarding/question_selection_service.py
```
Умный алгоритм выбора вопросов

### 3. Изучи AI анализ
```
systems/analysis/analysis_worker_service.py
systems/analysis/ai_router.py
```
Как бот понимает твои ответы

### 4. Пойми хранение профилей
```
systems/profile/profile_storage_service.py
systems/profile/trait_evolution_service.py
```
Как хранится и эволюционирует твоя личность

### 5. Посмотри деплой
```
deployment/blue-green-deploy.sh
deployment/docker-compose.production.yml
```
Как это всё запускается в продакшн

---

## Термины (словарь)

- **Микросервис** - маленькая независимая программа, которая делает одну вещь
- **Event-driven** - архитектура, где сервисы общаются через события (записки)
- **Redis Streams** - доска объявлений для событий
- **PostgreSQL** - база данных для хранения структурированных данных
- **Qdrant** - векторная база данных для поиска похожих ответов
- **Blue-Green** - способ обновления без остановки сервиса
- **Circuit Breaker** - автоматический выключатель при ошибках
- **Outbox Pattern** - гарантированная доставка сообщений
- **Big Five** - модель личности из 5 черт (Openness, Conscientiousness, Extraversion, Agreeableness, Neuroticism)
- **FSM** - Finite State Machine (конечный автомат состояний)

---

## Дальнейшее чтение

1. **API_DOCUMENTATION.md** - подробные API всех сервисов
2. **ARCHITECTURE.md** - глубокое погружение в архитектуру
3. **RUNBOOK.md** - как управлять в продакшене

---

*Документ создан: 2025-10-01*
*Версия проекта: v1.0.0 (Production Ready)*
*Автор: Selfology Development Team*
