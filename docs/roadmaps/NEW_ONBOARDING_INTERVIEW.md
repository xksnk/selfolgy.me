# ИНТЕРВЬЮ: Новая система онбординга

**Дата**: 2025-11-29
**Формат**: Вопрос-ответ с записью

---

## ФАКТЫ (записываю твои ответы)

### Новое ядро
- **Файл**: `selfology_programs_v2.json`
- **Вопросов**: 674
- **Программ**: 29
- **Кластеров**: блоки внутри программ (например "Подумать о жизни" = 8 блоков)

### Старое ядро (АРХИВИРОВАТЬ)
- `selfology_final_sequenced.json` (1513)
- `selfology_intelligent_core.json` (693)
- и все остальные варианты

---

## ВОПРОСЫ

### 1. Где физически лежит НОВОЕ ядро?
**Ответ**:
- **Исходник**: `research/Ты методолог по дизайну рефлексивных опросников..md` (300KB)
- Это переработанные программы от методолога
- Формат: 29 программ → блоки → вопросы с пометками 📖/🤖
- Уже частично в: `intelligent_question_core/data/selfology_programs_v2.json`

### 2. Что такое "кластер" в твоём понимании?
**Ответ**:
- **Кластер = блок последовательных НЕРАЗРЫВНЫХ вопросов внутри программы**
- Нельзя нарушить порядок вопросов в кластере
- Нужно закончить кластер до конца
- Если прервал сессию - постоянно напоминать о незаконченном кластере

**ТРИ РЕЖИМА нового онбординга**:
1. **Умный AI режим** - автоматически подбирает лучший кластер для быстрого покрытия личности (создание цифрового отпечатка)
2. **Выбор программы** - пользователь выбирает 1 из 29 программ
3. **Закончить кластеры** - показывать ТОЛЬКО если есть незаконченные
   - При нажатии: выводит все вопросы + ответы кластера (освежить память)
   - Затем следующий непройденный вопрос

### 3. Связь программа ↔ кластер ↔ вопрос - как это работает?
**Ответ**:
```
Программа (29 шт) → Блоки/Кластеры (неразрывные) → Вопросы (последовательные)

Пример "Подумать о жизни":
├── Блок 1: "Здесь и сейчас" (4 вопроса)
├── Блок 2: "Люди рядом" (5 вопросов)
├── ...
└── Блок 8: "Образ будущего" (3 вопроса)
```

### 4. Старое ядро - что с ним делать?
**Ответ**: АРХИВИРОВАТЬ (всё что не `selfology_programs_v2.json`)

### 5. БД с ответами пользователей - очистить или сохранить?
**Ответ**: **A) Очистить полностью (новый старт)**
- Таблицы: `user_answers_new`, `answer_analysis`, `onboarding_sessions`
- Причина: старые вопросы несовместимы с новой структурой

### 6. Поле `format` (ai_only/book_only/both) - использовать?
**Ответ**: **НЕТ, игнорировать**
- Все 674 вопроса идут и в книгу, и в бота
- Поле format было ошибочно распарсено, не имеет значения

### 7. Метаданные вопросов (depth, energy, safety...) - нужны?
**Ответ**: **Метаданные на уровне БЛОКА достаточно**
- В `selfology_programs_v2.json` уже есть `block_metadata`:
  - `base_journey_stage` (ENTRY, EXPLORING...)
  - `base_depth_range` [SURFACE, CONSCIOUS]
  - `base_energy_dynamic` (OPENING, HEAVY...)
  - `base_safety_minimum`, `base_complexity_range`
- Вопросы наследуют метаданные от своего блока

### 8. Умный AI режим - как выбирать кластер?
**Ответ**: **A) Фиксированный список для MVP**
- Цель: покрывать домены, пополнять цифровой отпечаток
- Нужно: проанализировать каждый кластер и составить оптимальный порядок
- Потом: добавить умный подбор

**ГОТОВО**: Создан `cluster_sequence_v1.json`
- ENTRY: 32 кластера, 118 вопросов
- EXPLORATION: 120 кластеров, 402 вопроса
- INTEGRATION: 38 кластеров, 154 вопроса
- **ИТОГО: 190 кластеров, 674 вопроса**

---

## АНАЛИЗ

### Что имеем:
```
НОВОЕ ЯДРО: selfology_programs_v2.json
├── 29 программ
├── 190 кластеров (блоков)
├── 674 вопроса
└── block_metadata для каждого блока (depth, energy, safety...)

ПОРЯДОК: cluster_sequence_v1.json
├── Волна 1: ENTRY (32 кластера) - знакомство со всеми сферами
├── Волна 2: EXPLORATION (120 кластеров) - углубление
└── Волна 3: INTEGRATION (38 кластеров) - синтез
```

### Ключевые решения:
1. **Кластер = неразрывный блок** - нельзя прервать, нужно закончить
2. **3 режима**: Умный AI | Выбор программы | Закончить кластеры
3. **БД очищаем** - новый старт, старые ответы несовместимы
4. **Метаданные на уровне блока** - вопросы наследуют
5. **Поле format игнорируем** - все вопросы для бота

---

## ПЛАН МИГРАЦИИ

### Этап 1: Подготовка данных
- [x] Создать cluster_sequence_v1.json
- [ ] Очистить поле format из selfology_programs_v2.json (опционально)
- [ ] Архивировать старые JSON файлы в папку /archive

### Этап 2: База данных
- [ ] Создать backup текущей БД
- [ ] Очистить таблицы: user_answers_new, answer_analysis, onboarding_sessions
- [ ] Добавить таблицу user_cluster_progress (user_id, cluster_id, status, started_at, completed_at)
- [ ] Обновить схему для хранения block_id вместо question_json_id

### Этап 3: Рефакторинг кода
- [ ] Новый ClusterRouter (заменяет QuestionRouter)
  - load_sequence() - загрузка cluster_sequence_v1.json
  - get_next_cluster() - следующий по порядку
  - get_cluster_questions() - все вопросы кластера
- [ ] Обновить OnboardingOrchestrator
  - Три режима работы
  - Логика "закончить кластер"
  - Напоминания о незаконченных кластерах
- [ ] UI для выбора режима и программы

### Этап 4: Тестирование
- [ ] Unit тесты ClusterRouter
- [ ] Integration тест полного flow
- [ ] Ручное тестирование в Telegram

---

## ОБСУЖДЕНИЕ АРХИТЕКТУРЫ БД (в процессе)

### Контекст проблемы:
Для AI-анализа ответа нужен контекст:
- Текст вопроса (что спросили)
- Текст ответа (что ответил)
- Программа, кластер, позиция
- Метаданные блока (depth, energy, journey_stage)

### Варианты хранения:

**Вариант 1: Связанные таблицы (классика)**
- Таблица `questions` - все 674 вопроса с текстами
- Таблица `answers` - ответы + только ID вопроса (FK)
- При анализе: JOIN двух таблиц
- Плюс: нет дублирования
- Минус: каждый раз собирать из двух мест

**Вариант 2: Всё в одной таблице (денормализация)**
- Таблица `answers` содержит и текст вопроса, и текст ответа
- Плюс: один запрос - всё на месте
- Минус: текст дублируется (100 ответов = 100 копий вопроса)

**Вариант 3: Гибрид**
- Таблица `questions` как справочник
- В `answers` тоже копия текста вопроса
- Справочник для поиска/редактирования, копия для быстрого анализа

### ОТКРЫТЫЙ ВОПРОС:
**Нужна ли отдельная таблица с вопросами? Или достаточно JSON как источник истины, а в БД только ответы с копией текста вопроса?**

---

## СТАТУС СЕССИИ

**Дата**: 2025-11-29
**Прогресс**: ✅ БЕТА-ВЕРСИЯ ГОТОВА К ТЕСТИРОВАНИЮ
**Следующий шаг**: Тестирование /onboarding в Telegram

---

## ✅ СДЕЛАНО 29 НОЯБРЯ

### Архитектура БД (решено):
- **JSON как источник истины** для вопросов (не дублируем в PostgreSQL)
- **PostgreSQL только для ответов** с `question_id` ссылкой на JSON
- Созданы таблицы:
  - `selfology.user_answers_v2` (user_id, cluster_id, question_id, answer_text)
  - `selfology.user_cluster_progress` (user_id, cluster_id, status, progress)

### Новые компоненты:
- [x] `cluster_router.py` - загрузка 29 программ, 190 кластеров, 674 вопроса из JSON
- [x] `orchestrator_v2.py` - управление 3 режимами с сохранением в БД
- [x] Обновлён `selfology_controller.py` - новые handlers для v2
- [x] Исправлен конфиг БД (port 5434, selfology_user)
- [x] Бот запущен в screen сессии

### Бэкап:
- Git tag: `backup-before-v2-refactor`

---

## 🎯 ПЛАН НА 30 НОЯБРЯ

### Приоритет 1: Тестирование в Telegram
- [ ] Тест `/onboarding` - должно показать 3 режима
- [ ] Тест "Умный AI режим" - первый кластер из sequence
- [ ] Тест "Выбор программы" - список 29 программ
- [ ] Тест ответа на вопрос - сохранение в БД, следующий вопрос
- [ ] Тест завершения кластера - переход к следующему

### Приоритет 2: Исправить если сломано
- [ ] Если кнопки не работают - проверить callback handlers
- [ ] Если БД не сохраняет - проверить db_pool в orchestrator
- [ ] Если вопросы не загружаются - проверить путь к JSON

### Приоритет 3: Мониторинг (некритично)
- [ ] Ошибки `column aa.vectorization_status does not exist` - старая схема мониторинга
- [ ] Можно отключить или обновить таблицы

### Приоритет 4: Архивация
- [ ] Переместить старые JSON в `/archive`
- [ ] Очистить старые таблицы `user_answers_new`, `answer_analysis`

---

## 🚀 КАК ЗАПУСТИТЬ БОТА

```bash
# Проверить статус
screen -ls
pgrep -f selfology_controller.py

# Если не запущен:
cd /home/ksnk/microservices/critical/selfology-bot
docker exec redis redis-cli -n 1 DEL selfology:bot:instance_lock
screen -dmS selfology bash -c 'source venv/bin/activate && python selfology_controller.py 2>&1 | tee /tmp/bot.log'

# Смотреть логи:
tail -f /tmp/bot.log

# Подключиться к screen:
screen -r selfology
# (Ctrl+A, D чтобы отключиться)
```

---

## 📊 ТЕКУЩИЙ СТАТУС БОТА

```
✅ ClusterRouter: 29 программ, 190 кластеров, 674 вопроса
✅ OrchestratorV2: initialized
✅ Database: selfology-postgres:5434 connected
✅ ChatCoachService: ACTIVE with 6 components
✅ Polling: started
```
