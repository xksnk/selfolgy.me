# Быстрая Инструкция: Перенос Сгенерированных Вопросов

**Время**: ~5-10 минут
**Сложность**: Простое copy-paste

---

## 📋 ШАГ ЗА ШАГОМ

### 1. Открой скрипт для редактирования
```bash
nano /home/ksnk/n8n-enterprise/projects/selfology/scripts/extract_from_outputs.py
```

### 2. Найди Task Outputs в терминале

**Прокрути терминал ВВЕРХ** к моим сообщениям с Task results.

Ищи блоки вида:
```
<result>
<name>Task</name>
<output>```json
[
  {
    "id": "q_EDGE_001",
    ...
  }
]
```</output>
</result>
```

### 3. Копируй JSON данные

Для каждого из 13 блоков:

#### Блок 1: EDGE (75 вопросов)
- Найди: `Task: Generate EDGE questions`
- Скопируй JSON array: `[{...}, {...}, ...]`
- Вставь в скрипт: `EDGE_QUESTIONS = [{...}]`

#### Блок 2: SHADOW (40 вопросов)
- Найди: `Task: Generate SHADOW questions`
- Скопируй JSON array
- Вставь: `SHADOW_QUESTIONS = [{...}]`

#### Блок 3: CORE (25 вопросов)
- Найди: `Task: Generate CORE questions`
- Скопируй → `CORE_QUESTIONS = [{...}]`

#### Блок 4: HEALING (60 вопросов)
- Найди: `Task: Generate HEALING questions`
- Скопируй → `HEALING_QUESTIONS = [{...}]`

#### Блок 5: EMOTIONS (50 вопросов)
- Найди: `Task: Generate EMOTIONS questions`
- Скопируй → `EMOTIONS_QUESTIONS = [{...}]`

#### Блок 6: RELATIONSHIPS (50 вопросов)
- Найди: `Task: Generate RELATIONSHIPS questions`
- Скопируй → `RELATIONSHIPS_QUESTIONS = [{...}]`

#### Блок 7: GOALS (50 вопросов)
- Найди: `Task: Generate GOALS questions`
- Скопируй → `GOALS_QUESTIONS = [{...}]`

#### Блок 8: FEARS (30 вопросов)
- Найди: `Task: Generate FEARS questions`
- Скопируй → `FEARS_QUESTIONS = [{...}]`

#### Блок 9: VALUES (30 вопросов)
- Найди: `Task: Generate VALUES questions`
- Скопируй → `VALUES_QUESTIONS = [{...}]`

#### Блок 10: ENTRY (50 вопросов)
- Найди: `Task: Generate ENTRY questions`
- Скопируй → `ENTRY_QUESTIONS = [{...}]`

#### Блок 11: DEEPENING (100 вопросов)
- Найди: `Task: Generate DEEPENING questions`
- Скопируй → `DEEPENING_QUESTIONS = [{...}]`

#### Блок 12: INTEGRATING (50 вопросов)
- Найди: `Task: Generate INTEGRATING questions`
- Скопируй → `INTEGRATING_QUESTIONS = [{...}]`

#### Блок 13: TRANSFORMING (30 вопросов)
- Найди: `Task: Generate TRANSFORMING questions`
- Скопируй → `TRANSFORMING_QUESTIONS = [{...}]`

### 4. Сохрани скрипт (Ctrl+O, Enter, Ctrl+X в nano)

### 5. Запусти скрипт
```bash
cd /home/ksnk/n8n-enterprise/projects/selfology
python3 scripts/extract_from_outputs.py
```

### 6. Результат

Скрипт автоматически:
- ✅ Сохранит каждый блок в отдельный файл
- ✅ Объединит все в `generated_questions_v2.json`
- ✅ Исправит нестандартные домены
- ✅ Покажет статистику

---

## ⚡ АЛЬТЕРНАТИВА: Через другой скрипт

Если не хочешь редактировать Python:

```bash
# Запусти merge скрипт который читает из файлов
python3 scripts/final_merge_questions.py
```

Но СНАЧАЛА нужно создать 13 JSON файлов в `generated_blocks/`.

---

## 🎯 ЧТО ПОТОМ?

После успешного merge:

```bash
# 1. Валидация
python3 scripts/validate_questions_completeness.py

# 2. Статистика
python3 scripts/analyze_questions_metadata.py

# 3. Интеграция с existing
# (следующий скрипт который я создам)
```

---

## ❓ НУЖНА ПОМОЩЬ?

Скажи:
- **"Покажи пример для одного блока"** - покажу детально
- **"Создай файлы автоматом"** - я сделаю Write calls
- **"Есть другой способ?"** - предложу альтернативу

---

**Какой вариант выбираешь?** 🚀
