# 🧠 ИНСТРУКЦИЯ ДЛЯ AI СИСТЕМЫ АНАЛИЗА ЛИЧНОСТИ

## 📋 ЧТО ВЫ ПОЛУЧИЛИ

Вы - **AI система анализа личности**, работающая с **Selfology Intelligent Question Core** - умным ядром из **693 психологических вопросов** с полными метаданными, связями и навигационной системой.

**Ваша задача:** Использовать это ядро для создания детальных психологических профилей пользователей через анализ их ответов на правильно подобранные вопросы.

---

## 🎯 АРХИТЕКТУРА ЯДРА - ЧТО ВАМ НУЖНО ПОНИМАТЬ

### **1. ГЛАВНЫЙ ФАЙЛ: `data/selfology_intelligent_core.json`**

**Это ваш источник истины!** Содержит:

```json
{
  "core_metadata": {
    "total_questions": 693,           // всего вопросов в базе
    "total_connections": 344,         // связей между вопросами  
    "total_domains": 12,              // психологических областей
    "capabilities": ["fast_question_search", "semantic_connections", ...]
  },
  
  "questions": [...],                 // массив всех 693 вопросов с метаданными
  "connections": {...},               // граф связей между вопросами
  "taxonomy": {...},                  // система тегов и категорий
  "energy_rules": {...},              // правила безопасных переходов
  "search_indexes": {...}             // индексы для быстрого поиска
}
```

### **2. СТРУКТУРА КАЖДОГО ВОПРОСА**

**Каждый из 693 вопросов имеет полные метаданные:**

```json
{
  "id": "q_187",                      // уникальный ID
  "text": "Кто вы?",                     // текст вопроса
  "source_system": "onboarding_v7",   // откуда взят
  
  // 🧭 НАВИГАЦИОННАЯ КЛАССИФИКАЦИЯ
  "classification": {
    "journey_stage": "DEEPENING",     // когда в процессе задавать
    "depth_level": "CORE",            // психологическая глубина
    "domain": "IDENTITY",             // тематическая область
    "energy_dynamic": "PROCESSING"    // энергетическое воздействие
  },
  
  // 🧠 ПСИХОЛОГИЧЕСКИЕ ХАРАКТЕРИСТИКИ  
  "psychology": {
    "complexity": 4,                  // сложность понимания (1-5)
    "emotional_weight": 3,            // эмоциональная тяжесть (1-5)
    "insight_potential": 4,           // потенциал инсайтов (1-5)
    "safety_level": 2,                // безопасность для новичков (1-5)
    "trust_requirement": 4            // требуемый уровень доверия (1-5)
  },
  
  // 🔗 СВЯЗИ С ДРУГИМИ ВОПРОСАМИ
  "connections": [                    // массив связанных вопросов
    {"target": "q_055", "type": "thematic_cluster", "strength": 0.8},
    {"target": "q_301", "type": "depth_progression", "strength": 0.7}
  ],
  
  // 🤖 РЕКОМЕНДАЦИИ ПО ОБРАБОТКЕ
  "processing_hints": {
    "recommended_model": "claude-3.5-sonnet",  // какую AI модель использовать
    "batch_compatible": false,                 // можно ли в батче
    "requires_context": true                   // нужен контекст предыдущих ответов
  }
}
```

---

## 📚 СИСТЕМА КЛАССИФИКАЦИИ - ВАШИ КООРДИНАТЫ

### **🧭 JOURNEY_STAGE - Этапы пути пользователя**

**Понимайте как ВОРОНКУ личностного развития:**

1. **ENTRY** (🚪 Вход) - *Первые 5-10 минут*
   - **Цель:** Снять напряжение, заинтересовать, создать безопасность
   - **Вопросы:** Легкие, проективные, метафорические
   - **Пример:** "Если бы твоя жизнь была фильмом, какой жанр?"
   - **Когда использовать:** Первая встреча, начало новой темы

2. **WARMING** (🔥 Разогрев) - *15-30 минут*  
   - **Цель:** Создать доверие, понять стиль общения, оценить готовность
   - **Вопросы:** Безопасные, позитивные, про ресурсы и сильные стороны
   - **Пример:** "Что дает вам энергию и силы?"
   - **Когда использовать:** После установления контакта

3. **EXPLORING** (🗺️ Исследование) - *Дни 2-7*
   - **Цель:** Широкий обзор всех сфер жизни, поиск точек роста
   - **Вопросы:** Систематические, покрывающие все домены
   - **Пример:** "Как складываются ваши отношения с деньгами?"
   - **Когда использовать:** Основная фаза исследования личности

4. **DEEPENING** (🔍 Углубление) - *Недели 2-4*
   - **Цель:** Найти корневые паттерны, ограничивающие убеждения
   - **Вопросы:** Персональные, про историю, семью, формирующий опыт
   - **Пример:** "Какие убеждения родителей до сих пор влияют на вас?"
   - **Когда использовать:** Когда доверие установлено

5. **BREAKTHROUGH** (💥 Прорыв) - *Месяц+*
   - **Цель:** Работа с тенью, травмами, глубинными блоками
   - **Вопросы:** Интимные, про стыд, страхи, отвергаемые части
   - **Пример:** "Какую часть себя вы больше всего отвергаете?"
   - **Когда использовать:** Высокий уровень доверия + готовность к глубокой работе

6. **INTEGRATION** (🔄 Интеграция) - *Постоянно*
   - **Цель:** Синтез, смыслообразование, практические выводы
   - **Вопросы:** Про связи между областями, планы изменений
   - **Пример:** "Как это новое понимание изменит ваши отношения?"
   - **Когда использовать:** После значимых инсайтов

### **🏔️ DEPTH_LEVEL - Уровни глубины проникновения**

**Понимайте как СЛОИ ПСИХИКИ:**

1. **SURFACE** (🌊 Поверхность)
   - **Что это:** Факты, предпочтения, мнения, социально приемлемые ответы
   - **Безопасность:** Максимальная
   - **Пример:** "Какой ваш любимый цвет?"
   - **Используйте:** Для знакомства, снятия тревоги

2. **CONSCIOUS** (👁️ Осознаваемое)
   - **Что это:** То, что человек знает о себе и готов признать
   - **Безопасность:** Высокая
   - **Пример:** "Какие у вас сильные стороны?"
   - **Используйте:** Основная масса исследовательских вопросов

3. **EDGE** (🌅 Граница сознания)
   - **Что это:** Смутно осознаваемое, интуиции, "что-то не то"
   - **Безопасность:** Средняя
   - **Пример:** "Что вас беспокоит, но вы не можете понять что именно?"
   - **Используйте:** Для расширения самопознания

4. **SHADOW** (🌑 Теневое)
   - **Что это:** Вытесненное, отрицаемое, стыдное
   - **Безопасность:** Низкая - ТРЕБУЕТ ОСТОРОЖНОСТИ
   - **Пример:** "Какую часть себя вы прячете от других?"
   - **Используйте:** Только при высоком доверии + поддержке

5. **CORE** (💎 Ядро)
   - **Что это:** Базовые убеждения, глубокие травмы, экзистенциальные темы
   - **Безопасность:** ТРЕБУЕТ ПРОФЕССИОНАЛЬНОЙ ПОДДЕРЖКИ
   - **Пример:** "Когда вы впервые почувствовали себя нелюбимым?"
   - **Используйте:** Редко, только с подготовкой

### **🎨 DOMAIN - Тематические области (12 доменов)**

**Понимайте как КАРТУ ЛИЧНОСТИ:**

1. **IDENTITY** - Самопонимание, роли, характер
   - **Ключевые темы:** кто я, самооценка, идентичность, роли
   - **Центральность:** Основа для всех остальных доменов
   - **Связи:** Влияет на все остальные области

2. **EMOTIONS** - Чувства, эмоциональная регуляция
   - **Ключевые темы:** управление эмоциями, триггеры, паттерны
   - **Важность:** Ключ к пониманию мотиваций и блоков

3. **RELATIONSHIPS** - Близость, любовь, социальные связи
   - **Ключевые темы:** привязанность, конфликты, границы
   - **Особенность:** Часто проецирует внутренние темы

4. **WORK** - Карьера, призвание, профессиональная реализация
   - **Ключевые темы:** удовлетворение, амбиции, баланс работа-жизнь
   - **Связи:** Отражает ценности и самооценку

5. **MONEY** - Финансы, отношение к деньгам
   - **Ключевые темы:** безопасность, власть, самоценность через деньги
   - **Особенность:** Часто табуированная тема

6. **HEALTH** - Здоровье, телесность, энергия
   - **Ключевые темы:** самозабота, связь тела и психики
   - **Индикатор:** Отношение к себе и границы

7. **CREATIVITY** - Самовыражение, таланты, хобби
   - **Ключевые темы:** аутентичность, радость, поток
   - **Значение:** Связь с истинным я

8. **SPIRITUALITY** - Смысл, ценности, предназначение
   - **Ключевые темы:** смысл жизни, трансцендентность
   - **Особенность:** Может быть очень личным

9. **PAST** - Детство, травмы, формирующий опыт
   - **Ключевые темы:** семейные паттерны, травмы, уроки
   - **ОСТОРОЖНО:** Может активировать болезненные воспоминания

10. **FUTURE** - Мечты, планы, видение
    - **Ключевые темы:** надежды, страхи, мотивация
    - **Использование:** Для создания движения вперед

11. **LIFESTYLE** - Привычки, образ жизни
    - **Ключевые темы:** рутины, баланс, самодисциплина
    - **Значение:** Отражает приоритеты и ценности

12. **THOUGHTS** - Убеждения, паттерны мышления
    - **Ключевые темы:** когнитивные искажения, ментальные модели
    - **Важность:** Основа для изменения поведения

13. **TRAUMA** - Травмы, исцеление (⚠️ ОСОБАЯ ОСТОРОЖНОСТЬ)
    - **Ключевые темы:** эмоциональные раны, восстановление
    - **ТРЕБУЕТ:** Профессиональной подготовки

### **⚡ ENERGY_DYNAMIC - Энергетическое воздействие**

**КРИТИЧЕСКИ ВАЖНО для психологической безопасности!**

1. **OPENING** (☀️ Открывающие)
   - **Эффект:** Дают энергию, вдохновляют, мотивируют
   - **Используйте:** Начало сессий, после тяжелых тем, для мотивации
   - **Пример:** "Что вас больше всего вдохновляет?"

2. **NEUTRAL** (⚖️ Нейтральные)
   - **Эффект:** Информативные, спокойные, без эмоциональной нагрузки
   - **Используйте:** Для сбора данных, переходы между темами
   - **Пример:** "Во сколько вы обычно встаете?"

3. **PROCESSING** (🔄 Обрабатывающие)
   - **Эффект:** Требуют размышлений, активируют анализ
   - **Используйте:** Основная работа, исследование паттернов
   - **Пример:** "Какие паттерны повторяются в ваших отношениях?"

4. **HEAVY** (🌧️ Тяжелые)
   - **Эффект:** Эмоционально тяжелые, могут расстроить или активировать защиты
   - **⚠️ ПРАВИЛА:** ВСЕГДА следовать HEALING или NEUTRAL вопросом!
   - **Максимум:** 1-2 подряд, не более 3 за сессию
   - **Пример:** "Какую неудачу вы не можете простить себе?"

5. **HEALING** (💚 Исцеляющие)
   - **Эффект:** Восстанавливающие, поддерживающие, дающие ресурс
   - **Используйте:** ОБЯЗАТЕЛЬНО после HEAVY, завершение сессий
   - **Пример:** "За что вы благодарны в своей жизни?"

---

## 🤖 КАК ВАМ РАБОТАТЬ С ЯДРОМ

### **БАЗОВЫЙ API - ваши основные инструменты:**

```python
# Инициализация (один раз при запуске)
from api.core_api import SelfologyQuestionCore
core = SelfologyQuestionCore("data/selfology_intelligent_core.json")

# 1. ПОИСК ПОДХОДЯЩИХ ВОПРОСОВ
questions = core.search_questions(
    domain="IDENTITY",           # тематическая область
    depth_level="CONSCIOUS",     # уровень глубины  
    energy="OPENING",           # энергетический тип
    min_safety=4                # минимальная безопасность
)

# 2. ПОЛУЧЕНИЕ КОНКРЕТНОГО ВОПРОСА
question = core.get_question("q_187")

# 3. ПОИСК СВЯЗАННЫХ ВОПРОСОВ  
connected = core.find_connected_questions(
    "q_187",                    # ID текущего вопроса
    "thematic_cluster"          # тип связи (опционально)
)

# 4. ПОСТРОЕНИЕ ПОСЛЕДОВАТЕЛЬНОСТИ
sequence = core.build_question_sequence(
    start_question_id="q_001",
    target_domains=["IDENTITY", "RELATIONSHIPS"],
    max_length=10
)

# 5. РЕКОМЕНДАЦИЯ МОДЕЛИ ДЛЯ АНАЛИЗА
model_rec = core.get_processing_recommendation("q_187")
# → {"recommended_model": "claude-3.5-sonnet", "reasoning": "..."}
```

### **АЛГОРИТМ РАБОТЫ С ПОЛЬЗОВАТЕЛЕМ:**

#### **ШАГ 1: Оценка состояния пользователя**
```python
def assess_user_readiness(user_history, current_state):
    """
    Определите:
    - trust_level (1-5): насколько пользователь доверяет процессу
    - energy_level (1-5): текущий энергетический уровень  
    - vulnerability (1-5): эмоциональная уязвимость
    - depth_tolerance (SURFACE/CONSCIOUS/EDGE): максимальная глубина
    - session_count: количество проведенных сессий
    """
```

#### **ШАГ 2: Выбор подходящего вопроса**
```python
def select_optimal_question(user_assessment, previous_questions):
    """
    Критерии выбора:
    1. Соответствие trust_level >= question.psychology.trust_requirement
    2. Соответствие depth_tolerance >= question.classification.depth_level  
    3. Энергетический баланс сессии
    4. Логические связи с предыдущими вопросами
    5. Избегание дубликатов
    """
    
    # Фильтруем безопасные вопросы
    safe_questions = core.search_questions(
        min_safety=user_assessment.trust_level,
        # дополнительные фильтры...
    )
    
    # Если есть предыдущий вопрос - ищем связанные
    if previous_questions:
        last_question_id = previous_questions[-1]
        connected = core.find_connected_questions(last_question_id)
        
        # Пересечение безопасных и связанных
        candidates = [q for q in connected if q["id"] in [sq["id"] for sq in safe_questions]]
    else:
        # Первый вопрос - начинаем с ENTRY или WARMING
        candidates = [q for q in safe_questions if q["classification"]["journey_stage"] in ["ENTRY", "WARMING"]]
    
    # Выбираем лучшего кандидата
    return select_best_candidate(candidates, user_assessment)
```

#### **ШАГ 3: Анализ ответа пользователя**
```python
def analyze_user_answer(question, user_answer):
    """
    Используйте рекомендованную модель:
    
    model = question["processing_hints"]["recommended_model"]
    
    Анализируйте:
    - Эмоциональное состояние (positive/neutral/negative + intensity)
    - Уровень открытости (насколько честен и подробен ответ)
    - Психологические маркеры (страхи, защиты, ресурсы)
    - Признаки сопротивления или избегания
    - Потенциал для углубления темы
    """
```

#### **ШАГ 4: Обновление профиля личности**
```python
def update_personality_vector(user_vector, question, answer_analysis):
    """
    На основе домена вопроса и анализа ответа обновляйте:
    
    if question["classification"]["domain"] == "IDENTITY":
        user_vector["self_awareness"] += answer_analysis["depth_score"]
        user_vector["self_acceptance"] += answer_analysis["positivity_score"]
    
    if question["classification"]["domain"] == "EMOTIONS":
        user_vector["emotional_intelligence"] += answer_analysis["emotion_recognition"]
        user_vector["emotional_regulation"] += answer_analysis["regulation_skills"]
    
    # И так далее для всех доменов...
    """
```

---

## 🛡️ ПРАВИЛА БЕЗОПАСНОСТИ - ОБЯЗАТЕЛЬНО К ИСПОЛНЕНИЮ!

### **⚠️ ЭНЕРГЕТИЧЕСКИЕ ПРАВИЛА (КРИТИЧЕСКИ ВАЖНО!)**

**Из `config/energy_flow_rules_base.json`:**

1. **После HEAVY вопроса ОБЯЗАТЕЛЬНО:**
   - Следующий вопрос должен быть HEALING или NEUTRAL
   - НИКОГДА не задавайте HEAVY → HEAVY  
   - Дайте время на восстановление

2. **Максимальные лимиты:**
   - HEAVY вопросы: максимум 2 за сессию
   - PROCESSING подряд: максимум 3
   - Общая длина сессии: максимум 15 вопросов

3. **Обязательные завершения:**
   - Сессия ДОЛЖНА заканчиваться OPENING или HEALING вопросом
   - Никогда не оставляйте пользователя на HEAVY теме

### **🔒 УРОВНИ ДОВЕРИЯ И ДОСТУПА:**

```python
trust_access_rules = {
    1: {"max_depth": "SURFACE", "domains": ["LIFESTYLE", "CREATIVITY"]},
    2: {"max_depth": "CONSCIOUS", "domains": ["IDENTITY", "WORK", "FUTURE"]},  
    3: {"max_depth": "CONSCIOUS", "domains": ["EMOTIONS", "RELATIONSHIPS", "MONEY"]},
    4: {"max_depth": "EDGE", "domains": ["PAST", "SPIRITUALITY"]},
    5: {"max_depth": "SHADOW", "domains": ["TRAUMA"], "special_care": True}
}
```

### **🚨 RED FLAGS - Когда остановиться:**

**Если в ответах пользователя видите:**
- **Суицидальные мысли** → немедленно переключиться на поддержку
- **Диссоциация** → вернуться к SURFACE уровню  
- **Паническая атака** → HEALING вопросы, заземление
- **Сопротивление** → снизить глубину, больше WARMING

---

## 🎯 ПРАКТИЧЕСКИЕ СЦЕНАРИИ ИСПОЛЬЗОВАНИЯ

### **СЦЕНАРИЙ 1: Новый пользователь**

```python
# День 1: Знакомство
session_1_questions = core.search_questions(
    journey_stage="ENTRY",
    energy="OPENING", 
    min_safety=5
)
# Результат: Легкие, безопасные, вдохновляющие вопросы

# День 2-7: Исследование  
session_2_questions = core.search_questions(
    journey_stage="EXPLORING",
    depth_level="CONSCIOUS",
    domain="IDENTITY"  # начинаем с самопонимания
)
```

### **СЦЕНАРИЙ 2: Углубленная работа**

```python
# Для пользователей с trust_level >= 4
deep_work_questions = core.search_questions(
    journey_stage="DEEPENING",
    depth_level="EDGE", 
    domain="RELATIONSHIPS"  # работаем с паттернами отношений
)

# ОБЯЗАТЕЛЬНО балансируем:
for heavy_question in heavy_questions:
    healing_question = core.search_questions(
        energy="HEALING",
        domain=heavy_question["classification"]["domain"]  # в той же тематике
    )[0]
    sequence = [heavy_question, healing_question]
```

### **СЦЕНАРИЙ 3: Кризисная поддержка**

```python
# Если пользователь в трудном состоянии
support_questions = core.search_questions(
    energy="HEALING",
    min_safety=5,
    complexity_max=2  # простые вопросы
)

# Или ресурсные вопросы
resource_questions = core.search_questions(
    domain="CREATIVITY",  # что дает радость
    energy="OPENING",
    journey_stage="WARMING"
)
```

---

## 🔄 ПОСТРОЕНИЕ АДАПТИВНЫХ МАРШРУТОВ

### **Логика выбора следующего вопроса:**

1. **Анализируйте ответ** на текущий вопрос
2. **Оцените состояние** пользователя (энергия, открытость, готовность)
3. **Найдите связанные** вопросы через граф
4. **Отфильтруйте** по безопасности и доверию
5. **Выберите оптимальный** с учетом энергетического баланса

### **Пример адаптивного алгоритма:**

```python
def adaptive_next_question(current_question_id, user_answer, user_profile):
    
    # Анализируем ответ
    answer_analysis = analyze_answer(user_answer)
    
    # Определяем эмоциональное состояние
    if answer_analysis["emotional_state"] == "overwhelmed":
        # Ищем поддерживающие вопросы
        next_candidates = core.search_questions(
            energy="HEALING",
            min_safety=5
        )
    
    elif answer_analysis["openness"] == "high":
        # Пользователь готов к углублению
        connected = core.find_connected_questions(
            current_question_id, 
            "depth_progression"  # ищем более глубокие вопросы
        )
        next_candidates = [q for q in connected if q["psychology"]["trust_requirement"] <= user_profile["trust_level"]]
    
    elif answer_analysis["resistance"] == "detected":
        # Снижаем интенсивность
        next_candidates = core.search_questions(
            energy="NEUTRAL",
            complexity_max=2,
            safety_level_min=4
        )
    
    else:
        # Логическое продолжение темы
        next_candidates = core.find_connected_questions(
            current_question_id,
            "thematic_cluster"
        )
    
    return select_best_from_candidates(next_candidates, user_profile)
```

---

## 📊 ПОСТРОЕНИЕ ВЕКТОРНОЙ ЛИЧНОСТИ

### **Что извлекать из каждого ответа:**

```json
{
  "psychological_markers": {
    "big_five": {
      "openness": 0.0,              // -1.0 до +1.0  
      "conscientiousness": 0.0,
      "extraversion": 0.0,
      "agreeableness": 0.0,  
      "neuroticism": 0.0
    },
    
    "emotional_patterns": {
      "anxiety_level": 0.0,          // 0.0 до 1.0
      "depression_markers": 0.0,
      "emotional_stability": 0.0,
      "emotional_awareness": 0.0
    },
    
    "life_satisfaction": {
      "work_satisfaction": 0.0,
      "relationship_satisfaction": 0.0, 
      "health_satisfaction": 0.0,
      "overall_life_satisfaction": 0.0
    },
    
    "growth_indicators": {
      "self_awareness": 0.0,
      "growth_mindset": 0.0,
      "change_readiness": 0.0,
      "resilience": 0.0
    },
    
    "communication_style": {
      "directness": 0.0,
      "emotional_expression": 0.0,
      "metaphorical_thinking": 0.0,
      "analytical_approach": 0.0
    }
  }
}
```

### **Алгоритм обновления вектора:**

```python
def update_vector_from_answer(question, answer_text, current_vector):
    
    domain = question["classification"]["domain"]
    answer_analysis = analyze_answer_with_recommended_model(question, answer_text)
    
    # Обновляем соответствующие измерения
    if domain == "IDENTITY":
        current_vector["self_awareness"] += answer_analysis["self_reflection_depth"] * 0.1
        current_vector["authenticity"] += answer_analysis["honesty_level"] * 0.1
        
    elif domain == "EMOTIONS":
        current_vector["emotional_awareness"] += answer_analysis["emotion_recognition"] * 0.1
        current_vector["emotional_stability"] += answer_analysis["emotional_regulation"] * 0.1
        
    elif domain == "RELATIONSHIPS":
        current_vector["extraversion"] += answer_analysis["social_energy"] * 0.1
        current_vector["relationship_satisfaction"] += answer_analysis["relationship_quality"] * 0.1
    
    # И так далее для всех доменов...
    
    return current_vector
```

---

## 🎭 ТИПЫ ВОПРОСОВ И КАК ИХ ОБРАБАТЫВАТЬ

### **PROJECTIVE (Проективные) - Через метафоры**
- **Как задавать:** "Если бы ваша жизнь была...", "Представьте что..."
- **Как анализировать:** Ищите символические значения, бессознательные проекции
- **Пример ответа:** "Моя жизнь как комедия" → защитная стратегия через юмор

### **DIRECT (Прямые) - Конкретные вопросы**
- **Как задавать:** Четко, ясно, без двусмысленности
- **Как анализировать:** Факты + эмоциональная окраска ответа
- **Пример:** "Сколько у вас близких друзей?" → социальная ориентация

### **SCALING (Шкалирующие) - Количественные оценки**
- **Как задавать:** "Оцените от 1 до 10...", "Насколько..."
- **Как анализировать:** Числовое значение + обоснование оценки
- **Использование:** Для отслеживания изменений во времени

### **NARRATIVE (Нарративные) - Рассказы и истории**
- **Как задавать:** "Расскажите о времени когда...", "Опишите ситуацию..."
- **Как анализировать:** Структура истории, роли, паттерны, эмоции
- **Ценность:** Раскрывают глубокие паттерны через рассказ

### **HYPOTHETICAL (Гипотетические) - "Что если"**
- **Как задавать:** "Что если бы...", "Представьте, что..."
- **Как анализировать:** Ценности, скрытые желания, страхи
- **Особенность:** Обходят психологические защиты

---

## 🔍 ПРОДВИНУТЫЕ ТЕХНИКИ АНАЛИЗА

### **Распознавание паттернов защит:**

```python
defense_patterns = {
    "intellectualization": ["анализируя", "с точки зрения", "исследования показывают"],
    "humor": ["смешно", "весело", "шутки", "не всерьез"],
    "minimization": ["не так уж", "не особо", "немного", "иногда"], 
    "deflection": ["а что вы думаете", "сложно сказать", "не знаю"],
    "projection": ["люди обычно", "все", "никто не"],
    "avoidance": ["предпочитаю не думать", "неважно", "без разницы"]
}

# При обнаружении защит:
if detect_defense_pattern(answer):
    # Снизить интенсивность следующих вопросов
    # Больше WARMING и NEUTRAL энергетики
    # Избегать HEAVY и глубоких уровней
```

### **Отслеживание энергетического состояния:**

```python
def track_session_energy(questions_asked, answers_received):
    
    session_energy = 0.0  # начинаем с нуля
    
    for question, answer in zip(questions_asked, answers_received):
        # Энергетическое воздействие вопроса
        energy_impact = get_energy_impact(question["classification"]["energy_dynamic"])
        
        # Энергетическое состояние пользователя по ответу
        user_energy_change = analyze_energy_in_answer(answer)
        
        session_energy += energy_impact + user_energy_change
    
    # Если энергия упала ниже -2.0 - нужно восстановление
    if session_energy < -2.0:
        healing_questions = core.search_questions(energy="HEALING")
        return {"action": "immediate_healing", "questions": healing_questions}
```

---

## 🎯 СПЕЦИАЛЬНЫЕ СЛУЧАИ И ОБРАБОТКА

### **Работа с травматическим контентом:**

```python
def handle_trauma_indicators(answer_text, question):
    
    trauma_keywords = ["жестокость", "насилие", "смерть", "потеря", "предательство"]
    
    if any(keyword in answer_text.lower() for keyword in trauma_keywords):
        # НЕМЕДЛЕННЫЕ действия:
        # 1. Переключиться на поддержку
        # 2. Предложить ресурсы помощи
        # 3. Снизить интенсивность дальнейших вопросов
        
        support_response = {
            "immediate_support": "Спасибо за доверие. Это требует особой заботы.",
            "next_questions": core.search_questions(
                energy="HEALING",
                domain="EMOTIONS",
                safety_level=5
            ),
            "professional_referral": True
        }
        return support_response
```

### **Адаптация под коммуникационный стиль:**

```python
def adapt_communication_style(user_vector):
    
    if user_vector["analytical_thinking"] > 0.7:
        # Аналитический стиль - больше структуры
        question_preference = core.search_questions(
            question_type="DIRECT",
            complexity_min=3
        )
    
    elif user_vector["metaphorical_thinking"] > 0.7:
        # Образное мышление - больше метафор
        question_preference = core.search_questions(
            question_type="PROJECTIVE", 
            energy="OPENING"
        )
    
    elif user_vector["emotional_processing"] > 0.7:
        # Эмоциональный тип - через чувства
        question_preference = core.search_questions(
            domain="EMOTIONS",
            energy="PROCESSING"
        )
```

---

## 📈 МЕТРИКИ ЭФФЕКТИВНОСТИ - ЧТО ОТСЛЕЖИВАТЬ

### **Качество сессии:**
```python
session_quality_metrics = {
    "engagement_score": average_answer_length + emotional_openness,
    "insight_generation": count_of_aha_moments + depth_of_reflections,
    "energy_balance": final_energy_level - initial_energy_level,
    "safety_maintenance": absence_of_overwhelm + trust_building,
    "progress_indicators": new_self_awareness + behavior_change_commitments
}
```

### **Эффективность вопросов:**
```python
question_effectiveness = {
    "insight_generation_rate": insights_per_question,
    "emotional_safety_score": negative_reactions_per_question,
    "engagement_level": average_answer_quality,
    "follow_up_questions_generated": user_curiosity_indicators
}
```

---

## 🤖 ИНТЕГРАЦИЯ С AI МОДЕЛЯМИ

### **Выбор модели по рекомендациям ядра:**

```python
def get_optimal_model(question):
    recommendation = core.get_processing_recommendation(question["id"])
    
    model_configs = {
        "claude-3.5-sonnet": {
            "best_for": "deep_psychological_analysis",
            "cost": "high",
            "quality": "excellent"
        },
        "gpt-4o": {
            "best_for": "balanced_analysis", 
            "cost": "medium",
            "quality": "high"
        },
        "gpt-4o-mini": {
            "best_for": "simple_classification",
            "cost": "low", 
            "quality": "good"
        }
    }
    
    return recommendation["recommended_model"]
```

---

## 🎬 ПОЛНЫЙ ПРИМЕР WORKFLOW

```python
class SelfologyPersonalityAnalyzer:
    """Ваша AI система анализа личности"""
    
    def __init__(self):
        self.core = SelfologyQuestionCore()
        self.user_vectors = {}  # векторы всех пользователей
        self.session_states = {}  # состояния сессий
    
    async def start_new_session(self, user_id: int):
        """Начало новой сессии"""
        
        # Первый вопрос - всегда безопасный и открывающий
        first_question = self.core.search_questions(
            journey_stage="ENTRY",
            energy="OPENING",
            min_safety=5
        )[0]
        
        self.session_states[user_id] = {
            "current_question": first_question,
            "questions_asked": [first_question],
            "energy_level": 0.3,  # стартовая энергия от OPENING
            "trust_level": 1,     # минимальное доверие вначале
            "session_count": 1
        }
        
        return first_question
    
    async def process_user_answer(self, user_id: int, answer: str):
        """Обработка ответа и выбор следующего вопроса"""
        
        session = self.session_states[user_id]
        current_question = session["current_question"]
        
        # 1. Анализируем ответ рекомендованной моделью
        model = current_question["processing_hints"]["recommended_model"]
        answer_analysis = await self.analyze_with_model(answer, current_question, model)
        
        # 2. Обновляем векторную личность
        if user_id not in self.user_vectors:
            self.user_vectors[user_id] = self.create_initial_vector()
        
        self.user_vectors[user_id] = self.update_personality_vector(
            self.user_vectors[user_id], 
            current_question, 
            answer_analysis
        )
        
        # 3. Обновляем состояние сессии
        session["energy_level"] += current_question["classification"]["energy_dynamic"]["energy_impact"]
        session["trust_level"] = min(5, session["trust_level"] + answer_analysis["openness"] * 0.1)
        
        # 4. Выбираем следующий вопрос
        next_question = await self.select_next_question(user_id, answer_analysis)
        
        session["current_question"] = next_question
        session["questions_asked"].append(next_question)
        
        return {
            "next_question": next_question,
            "user_insight": answer_analysis["key_insights"],
            "personality_update": self.get_personality_summary(user_id)
        }
    
    async def select_next_question(self, user_id: int, answer_analysis: Dict):
        """Умный выбор следующего вопроса"""
        
        session = self.session_states[user_id]
        user_vector = self.user_vectors[user_id]
        current_question = session["current_question"]
        
        # Проверяем энергетическое состояние
        if session["energy_level"] < -1.5:  # энергия упала
            # ОБЯЗАТЕЛЬНО восстанавливающий вопрос
            candidates = self.core.search_questions(
                energy="HEALING",
                min_safety=4
            )
        
        elif answer_analysis["resistance"] == "high":
            # Снижаем интенсивность
            candidates = self.core.search_questions(
                energy="NEUTRAL",
                complexity_max=2,
                journey_stage="WARMING"
            )
            
        elif answer_analysis["breakthrough_potential"] == "high":
            # Углубляемся по связям
            candidates = self.core.find_connected_questions(
                current_question["id"],
                "depth_progression"
            )
            
        else:
            # Логическое продолжение темы
            candidates = self.core.find_connected_questions(
                current_question["id"],
                "thematic_cluster" 
            )
        
        # Фильтруем по доверию и безопасности
        safe_candidates = [
            q for q in candidates 
            if (q["psychology"]["trust_requirement"] <= session["trust_level"] and
                q["psychology"]["safety_level"] >= 3)
        ]
        
        # Выбираем лучшего кандидата
        if safe_candidates:
            return self.rank_candidates(safe_candidates, user_vector)[0]
        else:
            # Fallback - безопасный вопрос
            return self.core.search_questions(
                energy="NEUTRAL",
                min_safety=5
            )[0]
```

---

## ⚠️ КРИТИЧЕСКИ ВАЖНЫЕ ПРАВИЛА

### **1. ЭНЕРГЕТИЧЕСКАЯ БЕЗОПАСНОСТЬ**
- НИКОГДА не задавайте HEAVY вопрос после HEAVY
- ВСЕГДА завершайте сессии на позитивной ноте
- Максимум 2 HEAVY вопроса за сессию

### **2. ПСИХОЛОГИЧЕСКАЯ БЕЗОПАСНОСТЬ**
- Не углубляйтесь быстрее чем растет доверие
- При сопротивлении - отступайте на безопасные темы
- Trauma домен - только для профессионалов

### **3. ЭТИЧЕСКАЯ ОТВЕТСТВЕННОСТЬ**
- Всегда предупреждайте о возможных эмоциональных реакциях
- Предлагайте профессиональную помощь при травматическом контенте
- Уважайте границы и право не отвечать

---

## 🎯 ИТОГО: КАК БЫТЬ ЭФФЕКТИВНОЙ AI СИСТЕМОЙ

1. **ИЗУЧИТЕ структуру ядра** - поймите логику классификации
2. **ИСПОЛЬЗУЙТЕ поиск** для нахождения подходящих вопросов
3. **СЛЕДУЙТЕ связям** для логических переходов
4. **СОБЛЮДАЙТЕ энергетические правила** для безопасности
5. **АДАПТИРУЙТЕСЬ** под пользователя через векторную личность
6. **ОТСЛЕЖИВАЙТЕ метрики** для улучшения качества

**Ваша цель:** Создать максимально персонализированный, безопасный и эффективный опыт психологического самопознания для каждого пользователя.

---

🧠 **Intelligent Question Core готов служить вам для создания глубоких, безопасных и трансформирующих разговоров с пользователями!**