# 🧠 ИСПРАВЛЕННЫЙ ПЛАН: Создание ядра умной базы вопросов

## ✅ ЧТО Я ПОНЯЛ:

**МОЯ ЗАДАЧА:** Создать **ЯДРО** - умную базу данных вопросов с метаданными, связями и тегами

**НЕ МОЯ ЗАДАЧА:** Системы анализа ответов, роутеры выбора вопросов, векторные личности пользователей

---

## 🎯 ЦЕЛЬ ПРОЕКТА (скорректированная):

Создать **"умное ядро вопросов"** - базу данных где:
- ✅ Каждый вопрос имеет **полные метаданные**
- ✅ Вопросы **связаны друг с другом** в паутину
- ✅ Есть **теги и классификация** для навигации
- ✅ **AI роутер** может выбирать подходящие модели для обработки

**Результат:** Другая AI-система сможет легко работать с этой базой для анализа пользователей

---

## 📋 ДЕТАЛЬНЫЙ ПЛАН РАБОТЫ

### **ДЕНЬ 1: Обогащение метаданных ($0.42, 3 часа)**

**Цель:** Каждый из 693 вопросов получает полный "паспорт"

**Что делаем:**

#### **Шаг 1.1: Дополняем структуру вопроса**
```json
// БЫЛО:
{
  "id": "q_187", 
  "text": "Кто вы?",
  "source_system": "onboarding_v7"
}

// СТАНЕТ:
{
  "id": "q_187",
  "text": "Кто вы?",
  "source_system": "onboarding_v7",
  
  // НАВИГАЦИОННЫЕ МЕТАДАННЫЕ
  "classification": {
    "journey_stage": "DEEPENING",     // когда в процессе задавать
    "depth_level": "CORE",            // насколько глубокий
    "domain": "IDENTITY",             // тематическая область
    "question_type": "DIRECT",        // тип формулировки
    "energy_dynamic": "PROCESSING"    // энергетическое воздействие
  },
  
  // КРИТИЧНО: АНАЛИЗ ОТВЕТОВ
  "answer_analysis": {
    "expected_format": "medium_text",           // короткий/средний/длинный текст
    "keywords": {
      "positive_indicators": ["я есть", "моя роль", "характер"],
      "negative_indicators": ["не знаю", "никто", "пустота"], 
      "red_flags": ["никто", "ничто", "не существую"],
      "depth_indicators": ["сущность", "глубина", "истинный"]
    },
    "typical_patterns": [
      "role_listing -> поверхностное понимание",
      "philosophical_answer -> глубокое самопознание",
      "deflection -> сопротивление вопросу"
    ]
  },
  
  // КРИТИЧНО: АДАПТИВНЫЕ ПРАВИЛА
  "requirements": {
    "min_trust_level": 4,              // требуемый уровень доверия (1-5)
    "min_session_depth": 3,            // не задавать в первых сессиях
    "prerequisite_questions": ["q_001", "q_055"], // должны быть отвечены до этого
    "follow_up_mapping": {
      "if_surface_answer": ["q_188", "q_189"],   // если поверхностный ответ
      "if_deep_answer": ["q_300", "q_301"],      // если глубокий ответ
      "if_resistance": ["q_050"]                 // если сопротивление
    }
  },
  
  // ДЛЯ AI ROUTER'А МОДЕЛЕЙ
  "processing_needs": {
    "analysis_complexity": "high",      // high/medium/low
    "recommended_model": "claude_sonnet", // какая модель лучше
    "batch_compatible": false,          // можно ли обрабатывать в батче
    "requires_context": true            // нужен контекст предыдущих ответов
  }
}
```

#### **Шаг 1.2: AI роутер моделей для обработки**
```python
# Роутер выбирает модель на основе метаданных вопроса:
if question.processing_needs.analysis_complexity == "high":
    model = "claude-3-5-sonnet"
elif question.processing_needs.batch_compatible:
    model = "gpt-4o-mini" 
else:
    model = "gpt-4o"
```

**Техническая реализация:**
- **28 батчей** × 25 вопросов = 693 вопроса
- **OpenAI GPT-4o-mini** для массовой классификации = $0.42
- **Сохранение в enhanced_questions.json**

---

### **ДЕНЬ 2: Построение паутины связей** 🕸️

**Цель:** Создать граф связей между всеми 693 вопросами

**Что делаем:**

#### **Шаг 2.1: Семантический анализ связей**
```python
# Для каждой пары вопросов определяем:
connection_analysis = {
    "q_001": {
        "q_055": {
            "connection_type": "thematic_cluster",    // тип связи
            "strength": 0.85,                        // сила связи (0-1) 
            "direction": "bidirectional",            // можно в обе стороны
            "transition_context": "identity_exploration" // контекст перехода
        },
        "q_108": {
            "connection_type": "logical_sequence",
            "strength": 0.72,
            "direction": "from_q001_to_q108",        // только в одну сторону
            "transition_context": "narrative_development"
        }
    }
}
```

#### **Шаг 2.2: Энергетическое картирование**
```python
# Правила энергетических переходов:
energy_flow_rules = {
    "HEAVY": {
        "can_follow": ["HEALING", "NEUTRAL"],        // что может идти после тяжелого
        "cannot_follow": ["HEAVY", "PROCESSING"],    // что нельзя
        "recovery_questions": ["q_501", "q_502"]     // восстанавливающие вопросы
    },
    "OPENING": {
        "can_lead_to": ["PROCESSING", "EXPLORING"],  // что можем открыть
        "optimal_next": "PROCESSING"                 // оптимальное продолжение
    }
}
```

**Результат:** `question_connections_graph.json` с полной картой связей

---

### **ДЕНЬ 3: Создание тегов и категорий** 🏷️

**Цель:** Создать систему навигации по вопросам

**Что делаем:**

#### **Шаг 3.1: Многоуровневая система тегов**
```json
{
  "tag_hierarchy": {
    
    // УРОВЕНЬ 1: ОСНОВНЫЕ КАТЕГОРИИ
    "primary_domains": [
      "IDENTITY", "RELATIONSHIPS", "WORK", "HEALTH", 
      "MONEY", "CREATIVITY", "SPIRITUALITY", "EMOTIONS"
    ],
    
    // УРОВЕНЬ 2: ПОДКАТЕГОРИИ  
    "subcategories": {
      "IDENTITY": ["self_concept", "roles", "values", "character", "narrative"],
      "RELATIONSHIPS": ["romantic", "family", "friends", "social", "boundaries"],
      "EMOTIONS": ["awareness", "regulation", "expression", "trauma", "healing"]
    },
    
    // УРОВЕНЬ 3: МИКРОТЕГИ
    "micro_tags": {
      "self_concept": ["self_worth", "self_image", "inner_voice", "authenticity"],
      "trauma": ["childhood", "relationship", "professional", "existential"]
    }
  }
}
```

#### **Шаг 3.2: Поисковые индексы**
```python
# Создаем индексы для быстрого поиска:
search_indexes = {
    "by_depth": {
        "SURFACE": [q_001, q_002, q_003],
        "CORE": [q_187, q_301, q_405]
    },
    "by_domain": {
        "IDENTITY": [q_001, q_055, q_187],
        "RELATIONSHIPS": [q_108, q_203, q_255]
    },
    "by_energy": {
        "OPENING": [q_001, q_010, q_025],
        "HEAVY": [q_187, q_301, q_450]
    }
}
```

---

### **ДЕНЬ 4: Финальная сборка умного ядра** 🎯

**Цель:** Объединить все компоненты в единую систему

**Что создаем:**

#### **Файловая структура:**
```
intelligent_question_core/
├── enhanced_questions.json        # 693 вопроса с полными метаданными  
├── question_connections.json      # граф связей между вопросами
├── question_taxonomy.json         # система тегов и категорий
├── energy_flow_rules.json         # правила энергетических переходов
├── ai_model_routing.json          # какую модель для какой задачи
└── question_search_indexes.json   # индексы для быстрого поиска
```

#### **API для работы с ядром:**
```python
class QuestionCoreAPI:
    """API для работы с умным ядром вопросов"""
    
    def get_question_by_id(self, question_id: str) -> Dict:
        """Получить вопрос со всеми метаданными"""
    
    def find_connected_questions(self, question_id: str, 
                               connection_type: str = None) -> List[Dict]:
        """Найти связанные вопросы"""
    
    def search_questions(self, domain: str = None, 
                        depth_level: str = None,
                        energy: str = None) -> List[Dict]:
        """Поиск вопросов по параметрам"""
    
    def get_optimal_sequence(self, start_questions: List[str],
                           target_domains: List[str]) -> List[str]:
        """Получить оптимальную последовательность вопросов"""
    
    def get_processing_recommendation(self, question_id: str) -> Dict:
        """Какую AI модель использовать для обработки этого вопроса"""
```

---

## 📊 ФИНАЛЬНАЯ СТРУКТУРА ВОПРОСА (все критичные поля):

```json
{
  "id": "q_187",
  "text": "Кто вы?",
  "source_system": "onboarding_v7",
  
  // 1. НАВИГАЦИЯ - когда и как задавать
  "navigation": {
    "journey_stage": "DEEPENING",
    "depth_level": "CORE", 
    "domain": "IDENTITY",
    "energy_dynamic": "PROCESSING"
  },
  
  // 2. АНАЛИЗ ОТВЕТОВ - как понимать ответы
  "answer_hints": {
    "expected_format": "medium_text",
    "positive_keywords": ["я есть", "моя роль"],
    "red_flags": ["никто", "не существую"],
    "patterns": ["philosophical->deep", "role_list->surface"]
  },
  
  // 3. АДАПТИВНЫЕ ПРАВИЛА - умная навигация  
  "requirements": {
    "min_trust": 4,
    "prerequisite": ["q_001"],
    "follow_ups": {
      "deep_answer": ["q_301"],
      "surface_answer": ["q_188"]
    }
  },
  
  // 4. СВЯЗИ - граф вопросов
  "connections": [
    {"id": "q_055", "type": "thematic", "strength": 0.8},
    {"id": "q_108", "type": "sequence", "strength": 0.6}
  ],
  
  // 5. ОБРАБОТКА - для AI роутера моделей
  "processing": {
    "complexity": "high",
    "model_recommendation": "claude_sonnet",
    "batch_compatible": false
  }
}
```

---

## 🗺️ ИСПРАВЛЕННЫЙ ПЛАН РАБОТЫ:

### **ДЕНЬ 1: Создание умных метаданных**

**Задача:** Превратить 693 простых вопроса в умную базу

**Что делаем:**
1. **AI классификация** всех вопросов ($0.42)
2. **Добавление метаданных** навигации и анализа
3. **AI роутер моделей** - какую модель для какого вопроса
4. **Сохранение в enhanced_questions.json**

**Результат:** 693 вопроса с полными метаданными для навигации

---

### **ДЕНЬ 2: Построение графа связей** 

**Задача:** Создать паутину взаимосвязанных вопросов

**Что делаем:**
1. **Семантический анализ** всех пар вопросов
2. **Построение графа связей** - кто к кому ведет
3. **Энергетическое картирование** - правила переходов
4. **Создание оптимальных путей** по темам

**Результат:** `question_connections.json` - граф из 693 узлов с умными связями

---

### **ДЕНЬ 3: Система тегов и поиска**

**Задача:** Создать навигацию по базе вопросов

**Что делаем:**
1. **Многоуровневая таксономия** тегов
2. **Поисковые индексы** для быстрого доступа  
3. **Фильтры и группировки** по разным критериям
4. **API для работы** с умным ядром

**Результат:** `question_taxonomy.json` + поисковая система

---

### **ДЕНЬ 4: Финальная сборка ядра**

**Задача:** Объединить все компоненты в работающую систему

**Что делаем:**
1. **Валидация данных** - проверка целостности связей
2. **Оптимизация структуры** для быстрого доступа
3. **Создание API** для внешних систем
4. **Документация** для разработчиков

**Результат:** Готовое умное ядро вопросов

---

## 🎯 ЧТО ПОЛУЧАЕТСЯ НА ВЫХОДЕ:

### **1. Enhanced Questions Database**
- 693 вопроса с **полными метаданными**
- **5-мерная классификация** каждого вопроса
- **Правила обработки** для AI систем

### **2. Question Web Graph** 
- **Граф связей** между всеми вопросами
- **Энергетические правила** переходов
- **Оптимальные маршруты** по темам

### **3. Navigation System**
- **Система тегов** для поиска
- **Поисковые индексы** для быстрого доступа
- **Фильтры** по всем параметрам

### **4. AI Processing Router**
- **Рекомендации моделей** для каждого вопроса
- **Оптимизация затрат** на обработку
- **Batch/individual** стратегии

---

## 💡 КАК БУДЕТ ИСПОЛЬЗОВАТЬСЯ:

### **Внешняя AI-система сможет:**

```python
# Найти подходящие вопросы
questions = core.search_questions(
    domain="IDENTITY", 
    depth_level="CONSCIOUS",
    energy="OPENING"
)

# Построить логичную последовательность  
sequence = core.build_sequence(
    start_questions=["q_001"],
    target_domains=["IDENTITY", "RELATIONSHIPS"],
    max_depth="EDGE"
)

# Понять как обрабатывать ответ
processing = core.get_processing_hints("q_187")
# → {"model": "claude_sonnet", "complexity": "high"}

# Найти следующий вопрос после ответа
next_question = core.find_follow_up(
    answered_question="q_187",
    answer_analysis={"type": "deep", "sentiment": "positive"}
)
```

---

## 📊 ТЕХНИЧЕСКОЕ ИСПОЛНЕНИЕ:

### **Инструменты:**
- ✅ **AI роутер моделей** уже создан
- ✅ **API ключи** сохранены безопасно  
- ✅ **Базовая структура** существует

### **Процесс:**
1. **Batch AI анализ** через оптимальные модели
2. **Построение связей** через семантический анализ
3. **Создание индексов** для быстрого поиска
4. **API интерфейс** для внешнего использования

### **Стоимость:** $0.42 за полную обработку 693 вопросов

---

## 🎯 ИТОГО: 

**Создаем ЯДРО** - умную базу вопросов с:
- ✅ Полными метаданными для навигации
- ✅ Графом связей для умных переходов
- ✅ Системой тегов для поиска
- ✅ Роутером моделей для оптимальной обработки

**НЕ создаем:** системы анализа пользователей - это задача для других компонентов

**Готов ли начинать с создания умных метаданных?** 🚀