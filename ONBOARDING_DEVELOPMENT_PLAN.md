# 🏗️ ПЛАН РАЗРАБОТКИ СИСТЕМЫ ОНБОРДИНГА SELFOLOGY

## 🎯 ОБЩАЯ КОНЦЕПЦИЯ

**Цель:** Создать умную двухуровневую систему онбординга:
1. **🧠 AI Роутер** - "распределяющая шляпа" вопросов (алгоритм "Умный Микс")
2. **🔬 Система анализа** - обработка ответов и создание психологического профиля

## 📋 АРХИТЕКТУРА ПО СИСТЕМАМ

### 🤖 1. **SELFOLOGY_CONTROLLER** (главный управляющий)
```
📂 selfology_controller.py
├── ✅ Существующие функции (бот, команды, FSM)
├── 🆕 Новая команда /onboarding  
├── 🆕 Новые FSM состояния онбординга
└── 🆕 Интеграция с OnboardingOrchestrator
```

### 🧠 2. **INTELLIGENT_QUESTION_CORE** (умное ядро вопросов)
```
📂 intelligent_question_core/
├── ✅ 693 вопроса в JSON (уже есть)
├── ✅ API для поиска вопросов (уже есть)  
├── 🆕 Флаги "на доработку" (добавить в JSON)
└── ✅ Метаданные для роутинга (уже есть)
```

### 🎯 3. **ONBOARDING_SYSTEM** (🆕 НОВАЯ СИСТЕМА)
```
📂 selfology_bot/services/onboarding/
├── 🆕 orchestrator.py          # Главный дирижер онбординга
├── 🆕 question_router.py       # Умный выбор вопросов  
├── 🆕 session_manager.py       # Управление сессиями
├── 🆕 fatigue_detector.py      # Детектор усталости
└── 🆕 config.py               # Настройки алгоритмов
```

### 🔬 4. **ANSWER_ANALYSIS_ENGINE** (🆕 ОТДЕЛЬНАЯ НАСТРАИВАЕМАЯ СИСТЕМА)
```
📂 selfology_bot/analysis/
├── 🆕 answer_analyzer.py       # Главный анализатор
├── 🆕 ai_model_router.py       # Роутер AI моделей
├── 🆕 trait_extractor.py       # Извлечение черт личности
├── 🆕 embedding_creator.py     # Создание векторов
├── 🆕 analysis_config.py       # 🔧 НАСТРОЙКИ (легко менять!)
└── 🆕 analysis_templates.py    # 🔧 ПРОМПТЫ (легко редактировать!)
```

### 🗄️ 5. **DATABASE_SYSTEM** (расширение существующей)
```
📂 selfology_bot/database/
├── ✅ service.py (уже есть)
├── ✅ user_dao.py (уже есть) 
├── 🆕 onboarding_dao.py        # Новые таблицы онбординга
├── 🆕 question_dao.py          # Связи вопрос-пользователь
└── 🆕 analysis_dao.py          # Результаты анализа
```

### 🎨 6. **MESSAGES_SYSTEM** (найти текущую структуру)
```
📂 Текущая структура: ТРЕБУЕТ ИССЛЕДОВАНИЯ
├── ✅ Существующие шаблоны (найти где)
├── 🆕 Шаблоны онбординга (добавить туда же)
└── 🆕 Debug режим с человечными названиями
```

### ⚡ 7. **VECTOR_SYSTEM** (интеграция с существующей)
```
📂 selfology_bot/vector/  
├── ✅ Qdrant integration (есть)
└── 🆕 personality_vectors.py   # Сохранение профилей
```

## 🔧 КЛЮЧЕВЫЕ ПРИНЦИПЫ

### ✅ **Согласованные решения:**
- **Алгоритм роутинга:** "Умный Микс" всех подходов
- **Векторная БД:** Сохраняем АНАЛИЗ ответов, не сами ответы  
- **Хранение вопросов:** Гибридный подход (минимум в БД + полные данные в JSON)
- **AI модели:** Claude для сложного анализа, GPT-4o для среднего, GPT-4o-mini для простого
- **Управление усталостью:** AI определяет + кнопки пользователя

### 📝 **Уточнения учтены:**
- **Шаблоны:** Найти текущую структуру и интегрироваться
- **Debug режим:** Человечные названия доменов/энергии (только для debug)
- **Модульность:** Анализ ответов - отдельный настраиваемый модуль
- **vector_search.py:** Убран из MVP (не нужен сейчас)

---

# 🚀 ДЕТАЛЬНЫЙ ПЛАН РАЗРАБОТКИ

## **ФАЗА 1: ОСНОВА** (1-2 дня)

### 🔍 **Задача 1.1: Исследование текущей структуры**
**Что делаю:**
1. Найти где сейчас хранятся шаблоны сообщений
2. Изучить структуру existующих FSM состояний  
3. Проверить текущие таблицы БД
4. Найти где импортируется intelligent_question_core

**Результат:** Полное понимание того, куда интегрироваться

**Требует согласования:** Где найдены шаблоны и как лучше интегрироваться

---

### 🏗️ **Задача 1.2: Создать OnboardingOrchestrator**
**Что делаю:**
```python
# Файл: selfology_bot/services/onboarding/orchestrator.py
class OnboardingOrchestrator:
    """Главный дирижер онбординга"""
    
    def __init__(self):
        self.question_router = QuestionRouter()
        self.session_manager = SessionManager()  
        self.fatigue_detector = FatigueDetector()
        # Анализатор добавим во 2 фазе
    
    async def start_onboarding(self, user_id: int) -> dict:
        """Начать онбординг для пользователя"""
        # 1. Создать сессию
        # 2. Выбрать первый вопрос
        # 3. Вернуть данные для отображения
    
    async def get_next_question(self, user_id: int) -> dict:
        """Получить следующий вопрос"""
        # 1. Проверить не устал ли пользователь
        # 2. Выбрать следующий вопрос через роутер
        # 3. Вернуть вопрос или сигнал завершения
    
    async def process_user_answer(self, user_id: int, answer: str) -> dict:
        """Обработать ответ пользователя"""  
        # 1. Сохранить ответ в БД
        # 2. Передать в анализатор (когда будет готов)
        # 3. Определить следующий шаг
```

**Требует согласования:** Интерфейс методов и структура возвращаемых данных

---

### 🧠 **Задача 1.3: Создать QuestionRouter (базовая версия)**
**Что делаю:**
```python
# Файл: selfology_bot/services/onboarding/question_router.py
class QuestionRouter:
    """Умный выбор вопросов - алгоритм Умный Микс"""
    
    def __init__(self):
        self.core_api = # подключить intelligent_question_core
        self.config = RouterConfig()
    
    async def select_first_question(self, user_id: int) -> dict:
        """Первый вопрос - всегда ENTRY + OPENING"""
        # Фильтр: journey_stage=ENTRY, energy=OPENING, НЕ flagged
    
    async def select_next_question(self, user_id: int, session_data: dict) -> dict:
        """Умный Микс алгоритм"""
        # 1. ФИЛЬТРЫ: исключить "на доработку" 
        # 2. АНАЛИЗ: история ответов пользователя
        # 3. АЛГОРИТМ: мозаика доменов + адаптивное углубление
        # 4. БАЛАНС: энергетический серфинг
        # 5. БЕЗОПАСНОСТЬ: никогда HEAVY -> HEAVY
    
    def _apply_safety_rules(self, candidates: list, last_questions: list) -> list:
        """Применить правила психологической безопасности"""
```

**Требует согласования:** Логика алгоритма "Умный Микс" и правила безопасности

---

### 🗄️ **Задача 1.4: Добавить новые FSM состояния**
**Что делаю:**
```python
# Добавить в selfology_controller.py
class OnboardingStates(StatesGroup):
    # Существующие состояния (не трогаем)
    gdpr_consent = State()
    assessment_intro = State() 
    
    # НОВЫЕ состояния онбординга
    onboarding_active = State()        # Активный процесс вопросов
    waiting_for_answer = State()       # Ожидаем ответ на вопрос  
    processing_answer = State()        # Анализируем ответ (пока просто сохраняем)
    onboarding_paused = State()        # Пауза по просьбе пользователя
    onboarding_complete = State()      # Онбординг завершен
```

**Требует согласования:** Названия состояний и переходы между ними

---

### 🏢 **Задача 1.5: Создать базовые таблицы БД**
**Что делаю:**
```sql
-- 1. Метаданные вопросов (гибридный подход)
CREATE TABLE questions_metadata (
    id SERIAL PRIMARY KEY,
    json_id VARCHAR(10) UNIQUE,     -- "q_001" ссылка на JSON
    domain VARCHAR(20),             -- для быстрой фильтрации
    depth_level VARCHAR(15),        
    energy VARCHAR(15),             
    is_flagged BOOLEAN DEFAULT false -- "на доработку"
);

-- 2. Сессии онбординга
CREATE TABLE onboarding_sessions (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id),
    started_at TIMESTAMP DEFAULT NOW(),
    completed_at TIMESTAMP,
    status VARCHAR(20) DEFAULT 'active' -- active, paused, completed
);

-- 3. Ответы пользователей (минимум для фазы 1)
CREATE TABLE user_answers (
    id SERIAL PRIMARY KEY,
    session_id INTEGER REFERENCES onboarding_sessions(id),
    question_json_id VARCHAR(10),   -- ссылка на JSON вопрос
    raw_answer TEXT,
    answered_at TIMESTAMP DEFAULT NOW()
);
```

**Требует согласования:** Структура таблиц и связи

---

## **ФАЗА 2: АНАЛИЗ** (2-3 дня)

### 🔬 **Задача 2.1: Создать AnswerAnalyzer (отдельный модуль)**
**Что делаю:**
```python
# Файл: selfology_bot/analysis/answer_analyzer.py
class AnswerAnalyzer:
    """Отдельная настраиваемая система анализа ответов"""
    
    def __init__(self):
        self.config = AnalysisConfig()          # 🔧 Настройки
        self.templates = AnalysisTemplates()    # 🔧 Промпты
        self.ai_router = AIModelRouter()        # Выбор модели
        self.trait_extractor = TraitExtractor() # Извлечение черт
    
    async def analyze_answer(self, question_data: dict, user_answer: str) -> dict:
        """Главный метод анализа"""
        # 1. Выбрать AI модель через роутер
        # 2. Проанализировать через AI
        # 3. Извлечь черты личности  
        # 4. Сохранить в БД и векторную БД
        # 5. Вернуть инсайты для роутера вопросов
```

**Требует согласования:** Структура входных/выходных данных и конфигурация

---

### **И так далее для каждой задачи...**

---

## 🤝 **ПРОЦЕСС СОГЛАСОВАНИЯ**

### **Для каждой задачи:**
1. **Показываю детальный план** того что буду делать
2. **Жду твоего согласования** или правок  
3. **Делаю только согласованную задачу**
4. **Показываю результат** и перехожу к следующей

### **Контроль контекста:**
- Один MD документ со всем планом
- По одной задаче за раз
- Четкие границы каждого шага
- Возможность корректировки на любом этапе

## 🎯 **ГОТОВ НАЧИНАТЬ?**

**Первая задача:** Задача 1.1 - Исследование текущей структуры

**Хочешь чтобы я начал с исследования того, где сейчас хранятся шаблоны и FSM состояния?**

Или есть корректировки к плану?