# 🔬 ФАЗА 2: СИСТЕМА АНАЛИЗА - ДЕТАЛЬНЫЙ ПЛАН

## 🎯 **ЦЕЛЬ ФАЗЫ 2:**
Создать "цифровую душу" - точное отражение уникальной личности человека через многослойный анализ ответов.

**Философия:** "Каждый ответ - это штрих в портрете души"

---

## 🔬 **ЗАДАЧА 2.1: AnswerAnalyzer - главный компонент**

### **1. Какие черты личности извлекать - Многослойная модель личности**

```python
COMPLETE_PERSONALITY_MODEL = {
    # ФУНДАМЕНТ - Big Five (всегда)
    "big_five": {
        "openness": 0.00-1.00,
        "conscientiousness": 0.00-1.00,
        "extraversion": 0.00-1.00,
        "agreeableness": 0.00-1.00,
        "neuroticism": 0.00-1.00
    },
    
    # ДИНАМИЧЕСКОЕ ЯДРО - меняется, но медленно
    "core_dynamics": {
        "resilience": 0.00-1.00,        # Способность восстанавливаться
        "authenticity": 0.00-1.00,      # Верность себе
        "growth_mindset": 0.00-1.00,    # Установка на развитие
        "emotional_granularity": 0.00-1.00,  # Тонкость чувств
        "cognitive_flexibility": 0.00-1.00,   # Гибкость мышления
        "self_compassion": 0.00-1.00,   # Принятие себя
        "meaning_making": 0.00-1.00     # Создание смыслов
    },
    
    # АДАПТИВНЫЕ ЧЕРТЫ - быстро меняются
    "adaptive_traits": {
        "current_energy": 0.00-1.00,    # Сейчас
        "stress_level": 0.00-1.00,      # Текущий стресс
        "openness_state": 0.00-1.00,    # Готовность открываться
        "creative_flow": 0.00-1.00,     # В потоке или нет
        "social_battery": 0.00-1.00     # Социальная энергия
    },
    
    # ДОМЕННЫЕ ПРОФИЛИ (на основе domain из вопросов)
    "domain_affinities": {
        "IDENTITY": 0.0-1.0,      # насколько человек фокусирован на самопознании
        "RELATIONSHIPS": 0.0-1.0,  # важность отношений
        "CAREER": 0.0-1.0,        # карьерная ориентация
        # ... для всех 12 доменов
    },
    
    # УНИКАЛЬНЫЕ МАРКЕРЫ - то, что делает человека особенным
    "unique_signature": {
        "thinking_style": "visual-metaphorical",
        "decision_pattern": "feel-think-feel",
        "energy_rhythm": "90min_cycles",
        "learning_edge": "through_experience",
        "love_language": "quality_time",
        "stress_response": "withdraw_then_process"
    }
}
```

### **2. Формат numerical scores - Гибридная система (0.0-1.0 + контекст)**

```python
trait_assessment = {
    "value": 0.75,          # Основное значение (0-1)
    "confidence": 0.92,     # Насколько мы уверены
    "variance": 0.08,       # Разброс между ответами
    "samples": 5,           # На скольких ответах основано
    "last_updated": "2025-01-09T10:30:00Z",
    
    # Контекстные модификаторы
    "percentile": 82,       # Позиция среди всех пользователей
    "direction": "increasing", # Тренд изменения
    "interpretation": "high", # low/medium/high/very_high
    
    # Для UI
    "display_formats": {
        "percentage": "75%",
        "stars": 4,          # из 5 звёзд
        "label": "Развитая", # Человеческое описание
        "color": "#4CAF50"   # Для визуализации
    }
}
```

### **3. Детальность анализа - Адаптивная глубина**

```python
ANALYSIS_DEPTH = {
    "surface": {
        "when": "first_5_questions OR energy_low OR trust < 30%",
        "length": "100-150 слов",
        "focus": "поддержка и принятие",
        "example": "Вижу вашу открытость к новому опыту и готовность к рефлексии..."
    },
    
    "flowing": {
        "when": "questions_6-25 OR energy_balanced OR trust 30-70%",
        "length": "200-300 слов",
        "focus": "инсайты и паттерны",
        "example": "Ваш ответ раскрывает интересное сочетание аналитического мышления..."
    },
    
    "deep_dive": {
        "when": "questions_26+ OR breakthrough_moment OR trust > 70%",
        "length": "400+ слов",
        "focus": "теневая работа и трансформация",
        "example": "Этот ответ - поворотный момент. Вижу, как прежние защитные механизмы..."
    }
}
```

---

## 🤖 **ЗАДАЧА 2.2: AI Model Router**

### **Матрица принятия решений - "Глубина × Важность × Контекст"**

```python
class SoulfulAIRouter:
    def select_ai_therapist(self, context):
        # Считаем оценку момента
        moment_score = (
            context["emotional_weight"] * 0.25 +      # Эмоциональная нагрузка
            context["insight_potential"] * 0.25 +     # Потенциал прорыва
            context["trust_level"] * 0.20 +           # Уровень доверия
            context["complexity"] * 0.15 +            # Сложность
            (5 - context["safety_level"]) * 0.15     # Деликатность темы
        )
        
        # CLAUDE-3.5-SONNET (Мудрый Наставник) - ~10% времени
        if any([
            moment_score >= 4.0,                      # Критический момент
            context["is_breakthrough"],                # Прорыв
            context["depth_level"] == "UNCONSCIOUS",  # Работа с тенью
            context["domain"] in ["SPIRITUALITY", "IDENTITY"],  # Экзистенциальное
            context["question_number"] in [25, 50, 75, 100]  # Вехи пути
        ]):
            return "claude-3.5-sonnet"
        
        # GPT-4O (Эмпатичный Друг) - ~75% времени
        elif moment_score >= 2.0:
            return "gpt-4o"
        
        # GPT-4O-MINI (Лёгкая Поддержка) - ~15% времени
        else:
            return "gpt-4o-mini"
```

### **Fallback стратегия - Каскадная с обогащением**

```python
FALLBACK_CHAIN = {
    "claude-3.5-sonnet": {
        "primary_fallback": "gpt-4o",
        "enrichment": "add_extra_context",  # Компенсируем промптом
        "timeout_ms": 3000
    },
    "gpt-4o": {
        "primary_fallback": "gpt-4o-mini",
        "enrichment": "simplified_analysis",
        "timeout_ms": 2000
    },
    "gpt-4o-mini": {
        "primary_fallback": "rule_based_analysis",  # Без AI
        "enrichment": "basic_only",
        "timeout_ms": 1000
    }
}
```

### **Логирование costs - Полная телеметрия**

```python
cost_tracking = {
    "request_id": "uuid",
    "timestamp": "2025-01-09T10:30:00Z",
    "user_id": 123,
    "question_id": "q_042",
    
    # Модель и стоимость
    "model_used": "gpt-4o",
    "model_requested": "claude-3.5-sonnet",  # Если был fallback
    "tokens_input": 450,
    "tokens_output": 320,
    "cost_usd": 0.0234,
    
    # Контекст для оптимизации
    "complexity_score": 3.2,
    "response_time_ms": 1250,
    "quality_score": 0.85,  # Из пользовательской оценки
    "was_fallback": true
}
```

---

## 📊 **ЗАДАЧА 2.3: Trait Extractor**

### **Дополнительные черты важны - Контекстно-зависимая иерархия**

```python
DOMAIN_SPECIFIC_TRAITS = {
    "RELATIONSHIPS": ["empathy", "boundaries", "intimacy_comfort", "conflict_style"],
    "CAREER": ["ambition", "leadership", "team_orientation", "risk_tolerance"],
    "IDENTITY": ["self_awareness", "authenticity", "identity_stability"],
    "EMOTIONS": ["emotional_granularity", "regulation_skill", "alexithymia_level"],
    "CREATIVITY": ["divergent_thinking", "artistic_sensitivity", "imagination"],
    "SPIRITUALITY": ["transcendence", "meaning_seeking", "faith_orientation"]
}

# Извлекаем только релевантные для domain
if question["classification"]["domain"] == "RELATIONSHIPS":
    extract_traits += DOMAIN_SPECIFIC_TRAITS["RELATIONSHIPS"]
```

### **Точность оценок - 2 знака + уровень уверенности**

```python
trait_value = round(raw_score, 2)  # 0.75, не 0.7523847

# С метаданными уверенности
trait_assessment = {
    "value": 0.75,
    "confidence": 0.92,  # Насколько мы уверены
    "variance": 0.08,    # Разброс между ответами
    "samples": 5,        # На скольких ответах основано
}
```

### **Нормализация - Умная многоуровневая 0.0-1.0**

```python
# Основная шкала: 0.0 - 1.0 для всех traits
normalized_trait = {
    "absolute_value": 0.75,      # 0-1 шкала
    "percentile": 82,            # Позиция среди всех пользователей
    "direction": "increasing",   # Тренд изменения
    "interpretation": "high"     # low/medium/high/very_high
}
```

---

## 🎨 **ЗАДАЧА 2.4: Analysis Templates**

### **Детальность промптов - Адаптивная под модель**

```python
# Для Claude (детальный, психологический)
CLAUDE_INSTRUCTIONS = """
Проанализируй ответ как опытный психолог-аналитик. Обрати внимание на:
- Скрытые паттерны и защитные механизмы
- Неосознаваемые мотивы и теневые аспекты
- Уровень аутентичности vs социальная желательность
- Эмоциональную окраску и энергетику текста
"""

# Для GPT-4o (структурированный, пошаговый)
GPT4_INSTRUCTIONS = """
Выполни анализ по шагам:
1. Определи эмоциональный тон
2. Оцени черты Big Five на основе содержания и стиля
3. Выяви domain-специфичные маркеры
4. Оцени уровень усталости
5. Сформулируй 2-3 ключевых инсайта
"""

# Для GPT-4o-mini (чеклист, бинарные оценки)
MINI_INSTRUCTIONS = """
Быстро определи:
☐ Эмоция: радость/грусть/страх/гнев/нейтрал
☐ Openness: низкая(<0.4)/средняя(0.4-0.7)/высокая(>0.7)
☐ Усталость: да/нет
☐ Ключевая тема: [одно слово]
"""
```

### **Структура JSON - Версионированная с обязательными полями**

```python
JSON_RESPONSE_SCHEMA = {
    "version": "2.0",  # ОБЯЗАТЕЛЬНО
    "timestamp": "ISO-8601",  # ОБЯЗАТЕЛЬНО
    
    "core_analysis": {  # ОБЯЗАТЕЛЬНО
        "emotional_state": {
            "primary": "confident",  # ОБЯЗАТЕЛЬНО
            "valence": 0.7,  # ОБЯЗАТЕЛЬНО (-1 до 1)
            "arousal": 0.3   # ОБЯЗАТЕЛЬНО (0 до 1)
        },
        "insights": {
            "main": "текст главного инсайта"  # ОБЯЗАТЕЛЬНО
        }
    },
    
    "traits": {  # ОБЯЗАТЕЛЬНО
        "big_five": {  # все 5 ОБЯЗАТЕЛЬНЫ
            "openness": 0.75,
            "conscientiousness": 0.62,
            "extraversion": 0.43,
            "agreeableness": 0.81,
            "neuroticism": 0.34
        }
    }
}
```

### **Версионирование промптов - Git-подобная система**

```python
PROMPT_VERSIONS = {
    "2.0.0": {  # Текущая stable
        "released": "2025-01-09",
        "changes": ["Added domain-specific traits", "Improved Claude prompts"],
        "prompts": {...}
    },
    "2.1.0-beta": {  # Тестовая
        "released": "2025-01-15",
        "changes": ["Added shadow work analysis", "New energy patterns"],
        "rollout_percentage": 10  # A/B тест на 10% пользователей
    }
}
```

---

## 📈 **ЗАДАЧА 2.5: Embedding Creator + Qdrant Integration**

### **1. Размер embedding - Многоуровневая стратегия**

```python
EMBEDDING_CONFIGS = {
    # Полный профиль - максимальная точность
    "full_personality": {
        "model": "text-embedding-3-large",
        "dimensions": 3072,  # Максимум от OpenAI
        "use_case": "deep_matching"
    },
    
    # Стандартный профиль - баланс
    "standard_personality": {
        "model": "text-embedding-3-small", 
        "dimensions": 1536,  # Стандарт OpenAI
        "use_case": "daily_operations"
    },
    
    # Быстрый поиск - оптимизация скорости
    "quick_match": {
        "model": "text-embedding-3-small",
        "dimensions": 512,  # Сжатый через API параметр
        "use_case": "real_time_search"
    }
}
```

### **2. Update стратегия - Инкрементальный merge с версионированием**

НЕ перезаписываем, а умно обновляем с сохранением истории:

```python
async def update_personality_vector(self, user_id, new_analysis):
    # 1. Создаём новый snapshot в evolution коллекции
    snapshot_id = f"{user_id}_{datetime.now().isoformat()}"
    
    # 2. Вычисляем дельту изменений
    delta = self.calculate_delta(current, new_analysis)
    
    # 3. Применяем взвешенное обновление основного профиля
    if delta["magnitude"] < 0.1:
        # Малые изменения - скользящее среднее
        updated = self.weighted_average(current, new_analysis, [0.8, 0.2])
    else:
        # Большие изменения - сохраняем как прорыв
        await self.save_breakthrough(user_id, current, new_analysis)
```

### **3. Поиск похожих - Многоуровневый с фильтрацией**

```python
async def find_similar_personalities(self, user_id: int):
    # 1. Быстрый префильтр через quick_match (512D)
    candidates = await qdrant_client.search(
        collection_name="quick_match",
        query_vector=user_profile["quick_vector"],
        limit=100
    )
    
    # 2. Точный поиск по полным векторам (1536D)
    detailed_matches = await qdrant_client.search(
        collection_name="personality_profiles", 
        query_vector=user_profile["full_vector"],
        limit=20
    )
    
    return self.rank_and_explain(detailed_matches)
```

---

## 🔗 **ЗАДАЧА 2.6: Интеграция с OnboardingOrchestrator**

### **Последовательность операций - Гибридный pipeline (Instant + Deep)**

```python
async def process_user_answer(self, user_id: int, question_id: str, answer: str):
    """Двухфазная обработка: быстрый ответ + глубокий анализ"""
    
    # ФАЗА 1: INSTANT (синхронно, <500ms)
    # 1.1 Сохранение ответа (критично!)
    answer_id = await self.onboarding_dao.save_user_answer(...)
    
    # 1.2 Быстрый анализ через GPT-4o-mini
    quick_analysis = await self.quick_analyzer.analyze(answer, timeout=400)
    
    # 1.3 Моментальный фидбек пользователю
    instant_response = {
        "answer_saved": True,
        "quick_insight": quick_analysis.get("emotion", "Обрабатываю..."),
        "next_question_id": self.router.get_next(quick_analysis)
    }
    
    # 1.4 Запускаем глубокий анализ в фоне
    asyncio.create_task(self._deep_analysis_pipeline(...))
    
    return instant_response
    
    # ФАЗА 2: DEEP (асинхронно, 2-10 секунд) 
    # Глубокий анализ через Claude/GPT-4 + векторное обновление
```

---

## 🗄️ **ЗАДАЧА 2.7: Новые таблицы для анализа**

### **1. Структура trait_scores JSONB - Полугибкая с обязательным ядром**

```sql
CREATE TABLE answer_analysis (
    id SERIAL PRIMARY KEY,
    user_answer_id INTEGER REFERENCES user_answers_new(id) ON DELETE CASCADE,
    analysis_version VARCHAR(10) NOT NULL,
    
    -- JSONB с обязательной структурой + расширениями
    trait_scores JSONB NOT NULL DEFAULT '{}' 
        CONSTRAINT valid_trait_structure CHECK (
            trait_scores ? 'version' AND
            trait_scores ? 'big_five' AND
            trait_scores ? 'timestamp'
        ),
    
    psychological_insights TEXT,
    emotional_state VARCHAR(30),
    fatigue_level FLOAT,
    next_question_hints JSONB,
    ai_model_used VARCHAR(30),
    processing_time_ms INTEGER,
    processed_at TIMESTAMP DEFAULT NOW(),
    
    -- Для отладки
    raw_ai_response JSONB,  # Сжатое хранение
    debug_priority SMALLINT DEFAULT 0
);
```

### **2. Хранение raw_ai_response - Умное с ротацией**

ДА, храним с умной стратегией:
- **7 дней**: Полные данные
- **30 дней**: Сжатые данные  
- **90 дней**: Только ключевые метрики
- **Вечно**: Важные моменты (прорывы, вехи)

### **3. Retention policy - "Горячее-Тёплое-Холодное-Архив"**

```python
RETENTION_POLICIES = {
    'answer_analysis': (7, 30, 90, 365, None),  # Никогда не удаляем
    'raw_ai_response': (3, 7, 30, 90, 180),     # Удаляем через 6 месяцев
    'debug_data': (1, 7, 30, 90, 90),           # Удаляем через 3 месяца
    'trait_evolution': (30, 90, 365, None, None) # Вечное хранение
}
```

---

## ⚡ **ЗАДАЧА 2.8: Qdrant Collection Setup**

### **Многоуровневые коллекции:**

```python
# 1. Главная коллекция - полные профили (1536D)
"personality_profiles" - основные профили пользователей

# 2. Эволюция личности - временные срезы (3072D для точности)  
"personality_evolution" - история изменений

# 3. Быстрый матчинг - упрощённые векторы (512D)
"quick_match" - для поиска в реальном времени

# 4. Мультивекторная коллекция - разные аспекты личности
"personality_facets" - cognitive/emotional/behavioral/social/creative
```

### **Векторный профиль - 4-слойная структура:**

```python
VECTOR_PERSONALITY_PORTRAIT = {
    # Слой 1: Мгновенный контекст (50 слов) - для быстрых операций
    "instant_essence": "Творческий интроверт-философ с высокой эмпатией...",
    
    # Слой 2: Рабочий профиль (200 слов) - для AI-диалогов
    "working_profile": "Человек с богатым внутренним миром...",
    
    # Слой 3: Глубинный портрет (500+ слов) - для серьёзной работы
    "deep_portrait": "[Подробный психологический портрет...]",
    
    # Слой 4: Эволюционная карта - как личность развивается
    "evolution_map": {
        "where_from": "Начинал с позиции осторожного наблюдателя",
        "where_now": "Учится доверять и открываться", 
        "where_going": "Движется к интеграции чувств и разума"
    }
}
```

---

## 🔄 **ПОЛНЫЙ ЦИКЛ РАБОТЫ ФАЗЫ 2**

### **Двухфазная обработка:**

```
1. Пользователь отвечает на вопрос
    ↓
2. INSTANT PHASE (<500ms):
   - Сохранение ответа
   - Быстрый анализ (GPT-4o-mini)
   - Следующий вопрос
   - Фидбек "обрабатываю..."
    ↓
3. DEEP PHASE (2-10 секунд в фоне):
   - AI роутер выбирает модель
   - Claude/GPT анализирует + все 17 метаданных
   - TraitExtractor извлекает черты
   - Анализ сохраняется в answer_analysis
   - EmbeddingCreator обновляет Qdrant
   - Уведомление "готов глубокий анализ!"
```

### **Performance - Instant фидбек + Deep анализ в фоне**

```python
PERFORMANCE_STRATEGY = {
    "instant_layer": {
        "время": "< 500ms",
        "что": "Эмоция, быстрая валидация, next question",
        "как": "GPT-4o-mini или кэш"
    },
    "deep_layer": {
        "время": "2-10 секунд", 
        "что": "Полный анализ, обновление векторов, инсайты",
        "как": "Claude/GPT-4 + Qdrant + полный pipeline"
    }
}
```

---

## 🎯 **ИТОГОВАЯ ФИЛОСОФИЯ**

**Мы создаём не просто алгоритм анализа, а цифрового психолога-друга, который:**
- Помнит всю историю человека
- Понимает контекст и нюансы  
- Чувствует правильный момент для глубины
- Растёт вместе с человеком
- Отражает уникальность каждой личности

**Каждое техническое решение продиктовано желанием создать максимально точное и живое отражение человеческой души в цифровом пространстве.**

---

## 📋 **ПЛАН РЕАЛИЗАЦИИ ФАЗЫ 2**

### **Task 2.1**: AnalysisConfig + AnalysisTemplates (настраиваемые файлы)
### **Task 2.2**: AIModelRouter (умный выбор модели)  
### **Task 2.3**: TraitExtractor (извлечение черт)
### **Task 2.4**: AnswerAnalyzer (главный анализатор)
### **Task 2.5**: EmbeddingCreator (векторы для Qdrant)
### **Task 2.6**: Database integration (новые таблицы)
### **Task 2.7**: OnboardingOrchestrator integration (двухфазная обработка)
### **Task 2.8**: Qdrant setup (коллекции и поиск)

**🚀 Готов начать пошаговую реализацию по этому плану!**