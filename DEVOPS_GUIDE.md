# DevOps Guide - Selfology Development

## Проблема решена: Теперь НЕТ необходимости перезапускать Docker при каждом изменении!

---

## Быстрый старт для разработчика

### Вариант 1: Docker с Hot Reload (РЕКОМЕНДУЕМЫЙ)

```bash
# Запустить dev окружение с автоматическим перезапуском
./dev.sh start

# Смотреть логи в реальном времени
./dev.sh logs

# Теперь просто меняй код - бот перезапустится автоматически!
```

**Что происходит:**
- Docker контейнер запускается ОДИН раз
- Код примонтирован через volumes (изменения видны сразу)
- `watchmedo` следит за изменениями `.py` файлов
- При изменении - автоматический перезапуск бота (1-2 секунды)
- Логи показываются в реальном времени

**Плюсы:**
- ✅ Полная изоляция (как в production)
- ✅ Не нужно устанавливать зависимости локально
- ✅ Hot reload работает автоматически
- ✅ Одинаковое окружение для всей команды

**Минусы:**
- ⏱️ Первая сборка займет 1-2 минуты
- ⏱️ Перезапуск ~2 секунды (vs мгновенно локально)

---

### Вариант 2: Локальный запуск БЕЗ Docker (САМЫЙ БЫСТРЫЙ)

```bash
# Один раз запустить - дальше только редактировать код
./run-local.sh

# Или через dev.sh
./dev.sh local
```

**Что происходит:**
- Бот запускается локально на твоей машине
- Подключается к Docker сервисам (postgres, redis, qdrant)
- `watchmedo` следит за изменениями
- Мгновенный перезапуск при изменении файлов

**Плюсы:**
- ⚡ САМЫЙ БЫСТРЫЙ вариант
- ⚡ Мгновенный перезапуск (<1 сек)
- 🐛 Легко подключить debugger
- 💻 Не расходует ресурсы на Docker

**Минусы:**
- 📦 Нужно установить зависимости локально (`pip install -r requirements.txt`)
- 🔧 Нужен Python 3.11

---

## Команды dev.sh

```bash
./dev.sh start      # Запустить с hot reload (Docker)
./dev.sh stop       # Остановить
./dev.sh logs       # Логи в реальном времени
./dev.sh status     # Проверить статус
./dev.sh shell      # Зайти в контейнер
./dev.sh build      # Пересобрать (после изменения requirements.txt)
./dev.sh local      # Запустить локально (БЕЗ Docker)
./dev.sh clean      # Очистить всё
```

---

## Production deployment

```bash
# Запуск в production
docker-compose -f docker-compose.selfology.yml up -d

# Просмотр логов
docker-compose -f docker-compose.selfology.yml logs -f

# Остановка
docker-compose -f docker-compose.selfology.yml down
```

**Отличия от dev:**
- ❌ Нет hot reload (стабильность важнее)
- ❌ Код НЕ примонтирован (полная изоляция)
- ✅ Ограничения по ресурсам (CPU/Memory)
- ✅ `DEBUG=false`
- ✅ Автоматический restart при падении

---

## Архитектура deployment

### Development (docker-compose.dev.yml)

```yaml
services:
  selfology-dev:
    build:
      dockerfile: Dockerfile.dev  # С watchmedo для hot reload
    volumes:
      - .:/app                    # Примонтирован весь код
    command: watchmedo auto-restart ...  # Автоперезапуск
    environment:
      - DEBUG=true
```

**Dockerfile.dev:**
- Устанавливает `watchdog` для мониторинга файлов
- Команда: `watchmedo auto-restart --pattern=*.py -- python selfology_controller.py`
- Игнорирует изменения в `logs/`, `venv/`, `__pycache__/`

### Production (docker-compose.selfology.yml)

```yaml
services:
  selfology-bot:
    build:
      dockerfile: Dockerfile      # Обычный, без hot reload
    volumes:
      - ./logs:/app/logs         # ТОЛЬКО логи
    command: python selfology_controller.py  # Простой запуск
    deploy:
      resources:
        limits: ...              # Ограничения ресурсов
```

**Dockerfile:**
- Минималистичный образ
- Код копируется ВНУТРЬ образа (не монтируется)
- Простой `CMD ["python", "selfology_controller.py"]`

---

## Workflow для разработчика

### Обычный день разработки:

```bash
# 1. Утром - запускаешь dev окружение
./dev.sh start

# 2. Открываешь логи в отдельном терминале
./dev.sh logs

# 3. Редактируешь код в VS Code / PyCharm
# ... меняешь файл selfology_bot/services/onboarding.py

# 4. Сохраняешь (Ctrl+S)
# ⚡ Автоматически:
#    - watchmedo замечает изменение
#    - Бот перезапускается (2 сек)
#    - Новый код работает!

# 5. Проверяешь в Telegram боте
# 6. Повторяешь шаги 3-5

# 7. Вечером - останавливаешь
./dev.sh stop
```

**НЕТ необходимости:**
- ❌ Перезапускать Docker контейнер
- ❌ Делать `docker-compose down/up`
- ❌ Ждать rebuild образа
- ❌ Думать о deployment во время разработки

---

## Когда нужен rebuild?

### Только при изменении зависимостей:

```bash
# Добавил новый пакет в requirements.txt?
./dev.sh build

# Изменил Dockerfile.dev?
./dev.sh build
```

### НЕ нужен rebuild при:
- ✅ Изменении любого `.py` файла
- ✅ Изменении templates/шаблонов
- ✅ Изменении конфигураций
- ✅ Добавлении новых модулей/файлов

---

## Troubleshooting

### Бот не перезапускается после изменений?

```bash
# Проверь логи
./dev.sh logs

# Должно быть что-то вроде:
# "Restarting process: python selfology_controller.py"
```

### Права доступа к файлам?

```bash
# Зайди в контейнер
./dev.sh shell

# Проверь права
ls -la /app

# Если проблемы - перезапусти
./dev.sh restart
```

### Медленный перезапуск?

```bash
# Используй локальный запуск
./dev.sh local

# Он НАМНОГО быстрее (мгновенный restart)
```

### Ошибка "network not found"?

```bash
# Проверь что запущены основные сервисы n8n
cd /home/ksnk/n8n-enterprise
docker-compose ps

# Должны работать: n8n-postgres, n8n-redis, qdrant
```

---

## Рекомендации

### Для быстрой разработки:
**Используй `./run-local.sh`** - мгновенный hot reload

### Для тестирования "как в production":
**Используй `./dev.sh start`** - полная изоляция в Docker

### Перед коммитом:
```bash
# Протестируй в Docker окружении
./dev.sh start
./dev.sh logs

# Убедись что всё работает
# Останови
./dev.sh stop
```

### Production deploy:
```bash
# Обновить код на сервере
git pull

# Пересобрать и перезапустить
docker-compose -f docker-compose.selfology.yml up -d --build

# Проверить логи
docker-compose -f docker-compose.selfology.yml logs -f
```

---

## Сравнение подходов

| Аспект | Local (`./run-local.sh`) | Docker Dev (`./dev.sh start`) | Production |
|--------|--------------------------|-------------------------------|------------|
| **Скорость перезапуска** | ⚡⚡⚡ Мгновенно | ⚡⚡ ~2 сек | - |
| **Изоляция** | ⚠️ Нет | ✅ Полная | ✅ Полная |
| **Hot Reload** | ✅ Да | ✅ Да | ❌ Нет |
| **Setup время** | 10 сек | 2 мин (первый раз) | 5 мин |
| **Debugger** | ✅ Легко | ⚠️ Сложнее | ❌ Нет |
| **Зависимости** | Нужны локально | В контейнере | В контейнере |
| **Ресурсы** | Минимум | Средне | Средне |

---

## Вопросы?

**"Как быстро начать работу?"**
```bash
./dev.sh start  # Запускается, дальше просто пиши код
```

**"Почему медленно?"**
```bash
./run-local.sh  # Используй локальный запуск
```

**"Как в production?"**
```bash
docker-compose -f docker-compose.selfology.yml up -d
```

**"Что-то сломалось?"**
```bash
./dev.sh clean   # Очисти всё
./dev.sh start   # Запусти заново
```

---

## Итого

### Главное преимущество:
**БОЛЬШЕ НЕТ необходимости перезапускать Docker при каждом изменении кода!**

### Workflow стал:
1. Запустил ОДИН раз: `./dev.sh start`
2. Редактируешь код
3. Сохраняешь - бот перезапускается автоматически
4. Радуешься жизни

### Экономия времени:
- Было: ~30 секунд на каждый `docker-compose down/up`
- Стало: ~2 секунды автоматический restart
- При 50 изменениях в день: экономия ~20 минут!

---

**Happy Coding! 🚀**
