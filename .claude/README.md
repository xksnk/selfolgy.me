# 🤖 Selfology AI Agent Configuration

Это конфигурация для интеллектуального AI агента, который помогает разрабатывать проект Selfology.

## 🎯 Что это дает?

### 1. **Контекстное понимание**
Агент автоматически определяет:
- Какая система затронута по ключевым словам
- Нужна ли миграция базы данных
- Какие тесты нужно запустить
- Есть ли риски для других систем

### 2. **Проактивные уточнения**
Если запрос неясен, агент спросит:
```
Пользователь: "Улучши вопросы"

Агент:
🤔 Уточни, что именно улучшить:
1. Базу данных вопросов (questions_metadata)?
2. Алгоритм выбора (Smart Mix)?
3. Анализ ответов (Analysis Worker)?
4. Формулировки в Telegram?
```

### 3. **Безопасность архитектуры**
Агент следит за:
- Event-driven коммуникацией между системами
- Правильной схемой БД (selfology, не public)
- Изоляцией систем друг от друга

---

## 📁 Структура

```
.claude/
├── agents/
│   └── selfology-architect.md     # Главный умный агент
├── commands/
│   ├── check-system.md            # Проверка состояния системы
│   └── test-system.md             # Тестирование конкретной системы
├── settings.json                  # Основная конфигурация
├── settings.local.json            # Локальные разрешения
└── README.md                      # Эта инструкция
```

---

## 🚀 Использование

### Базовый режим (автоматическое определение контекста)
```bash
# Агент сам поймет, что это касается question_selection
claude "Добавь новую стратегию роутинга REFLECTION"

# Агент сам поймет, что это кросс-системное изменение
claude "Когда пользователь отвечает, обнови его профиль души"

# Агент сам спросит детали
claude "Улучши систему"
```

### С конкретным агентом
```bash
# Использовать selfology-architect напрямую
claude --agent selfology-architect "твой запрос"
```

### Быстрые команды
```bash
# Проверка состояния всей системы
/check-system

# Тестирование конкретной системы
/test-system question_selection
/test-system soul_architect
```

---

## 🧠 Как агент понимает контекст?

### Ключевые слова → Системы

| Ключевые слова | Система | Тесты |
|----------------|---------|-------|
| вопрос, онбординг, Smart Mix | question_selection | tests/unit/systems/question_selection/ |
| душа, личность, профиль, черты | soul_architect | tests/unit/systems/soul_architect/ |
| AI, модель, GPT, Claude | ai_router | tests/unit/systems/ai_router/ |
| бот, telegram, кнопка | telegram_gateway | tests/unit/systems/telegram_gateway/ |
| миграция, таблица, БД | alembic | tests/integration/ |

### Decision Tree

```
Запрос пользователя
    ↓
Понятна задача? → НЕТ → Спросить детали
    ↓ ДА
Определить системы автоматически
    ↓
Одна система? → ДА → Изменить локально
    ↓ НЕТ
Использовать события между системами
    ↓
Нужна миграция? → ДА → Backup → Миграция → Тесты
    ↓ НЕТ
Внести изменения
    ↓
Запустить релевантные тесты
    ↓
Показать результат
```

---

## 📋 Примеры работы

### Пример 1: Простой запрос
```bash
Вы: "Добавь логирование в AI Router"

Агент анализирует:
✅ Система: ai_router
✅ Изменение: локальное
✅ Миграция: не нужна
✅ Тесты: tests/unit/systems/ai_router/

Агент делает:
1. Добавляет логирование в ai_router.py
2. Запускает pytest tests/unit/systems/ai_router/
3. Показывает результат
```

### Пример 2: Неясный запрос
```bash
Вы: "Сделай вопросы лучше"

Агент спрашивает:
🤔 Уточни, пожалуйста, что именно улучшить:

1. **Базу данных вопросов?**
   → Редактирование questions_metadata

2. **Алгоритм выбора вопросов (Smart Mix)?**
   → Изменения в question_selection/router.py

3. **Анализ ответов пользователей?**
   → Изменения в analysis_worker

4. **Формулировки в боте?**
   → Изменения в templates/ru/onboarding.json

Какой вариант нужен?
```

### Пример 3: Кросс-системное изменение
```bash
Вы: "После ответа на вопрос обнови профиль пользователя"

Агент анализирует:
⚠️ Кросс-системное изменение!
- Система A: question_selection (ответ)
- Система B: soul_architect (профиль)

Агент предупреждает:
"Такая логика уже существует через Analysis Worker:
1. question_selection публикует AnswerReceivedEvent
2. analysis_worker анализирует ответ
3. soul_architect обновляет профиль

Хочешь изменить существующий flow или добавить новый?"
```

---

## ⚙️ Конфигурация

### settings.json
Основные настройки:
- `systemPromptAppend` - контекст архитектуры Selfology
- `settingSources` - загрузка CLAUDE.md
- `permissions` - разрешения на команды
- `hooks` - хуки до/после редактирования
- `contextFiles` - важные файлы для контекста

### settings.local.json
Локальные разрешения (не в git):
- Разрешения на docker, psql, python команды
- Специфичные для вашей машины

---

## 🛡️ Правила безопасности

### ✅ Агент ВСЕГДА следит за:
1. Event-driven архитектурой (нет прямых вызовов между системами)
2. Схемой БД `selfology` (не `public`)
3. Запуском тестов после изменений
4. Созданием backup перед миграциями

### ❌ Агент НИКОГДА не сделает:
1. Прямой вызов между системами (только события)
2. Таблицы в схеме `public`
3. Изменения без тестов
4. `rm -rf` или `DROP DATABASE` без подтверждения

---

## 📚 Дополнительные ресурсы

Агент автоматически использует контекст из:
- `CLAUDE.md` - общий контекст проекта
- `REFACTORING_PROGRESS.md` - статус систем и архитектура
- `ARCHITECTURE_DIAGRAMS.md` - диаграммы взаимодействия
- `TESTING_STRATEGY.md` - стратегия тестирования

---

## 🎯 Что дальше?

### Попробуй:
```bash
# 1. Проверка системы
/check-system

# 2. Простое изменение
claude "Добавь комментарий в ai_router.py объясняющий логику выбора модели"

# 3. Сложное изменение
claude "Добавь новую стратегию роутинга DEEP_REFLECTION для метакогнитивных вопросов"

# 4. Неясный запрос (агент сам спросит детали)
claude "Улучши онбординг"
```

---

**Теперь агент понимает контекст и помогает безопасно разрабатывать Selfology! 🚀**
